<Window x:Class="BbxDeployer.Views.ProjectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Destination Project"
        Width="720"
        Height="310"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="18">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Display Name" VerticalAlignment="Center"/>
        <TextBox x:Name="DisplayNameBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10" Padding="6"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Repository Root" VerticalAlignment="Center"/>
        <TextBox x:Name="RepositoryRootBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" Padding="6"/>
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Game Project" VerticalAlignment="Center"/>
        <TextBox x:Name="UnityProjectRootBox" Grid.Row="2" Grid.Column="1" Padding="6"/>
        <TextBlock Grid.Row="3"
                   Grid.ColumnSpan="2"
                   Text="For a new project, set Game Project to a direct child of Repository Root. Run Preview to detect installed Unity versions and choose one on the project card."
                   Foreground="#64748B"
                   TextWrapping="Wrap"
                   Margin="0,14,0,0"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="90" Margin="0,0,8,0" IsDefault="True" Click="Save_Click"/>
            <Button Content="Cancel" Width="90" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
