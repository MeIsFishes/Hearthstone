<UserControl x:Class="BbxEditor.Wpf.Views.InspectorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource SidebarBackgroundBrush}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,0,0,0">
        <Grid Margin="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Margin="2,4,2,12">
                <TextBlock Text="INSPECTOR" FontSize="10" FontWeight="Bold"
                           Foreground="{StaticResource AccentHoverBrush}" />
                <TextBlock x:Name="InspectorTitle" Text="Inspector" Style="{StaticResource SectionTitleStyle}" Margin="0,3,0,0" />
            </StackPanel>
            <Border Grid.Row="1" x:Name="EmptyText" Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14">
                <StackPanel>
                    <TextBlock x:Name="EmptyTitle" Text="No Task Selected" FontWeight="SemiBold" />
                    <TextBlock x:Name="EmptyDescription" Text="Select a task node in the Timeline or Behavior Tree to edit its fields."
                               Style="{StaticResource CaptionTextStyle}" TextWrapping="Wrap" Margin="0,5,0,0" />
                </StackPanel>
            </Border>
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel x:Name="FieldsPanel" Margin="0,0,4,0" />
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
