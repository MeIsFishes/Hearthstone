<Window x:Class="BbxEditor.Wpf.Views.TaskSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Select Node Type" Width="680" Height="620" MinWidth="580" MinHeight="480"
        Style="{StaticResource {x:Type Window}}"
        Background="{StaticResource AppBackgroundBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        WindowStyle="None" ResizeMode="CanResizeWithGrip">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="42" ResizeBorderThickness="6" CornerRadius="8"
                            GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Border Background="{StaticResource AppBackgroundBrush}"
            BorderBrush="{StaticResource BorderStrongBrush}" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                    CornerRadius="8,8,0,0">
                <Grid Margin="14,0,6,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding DialogTitle}" FontWeight="SemiBold" />
                    <Button Grid.Column="1" Content="×" Width="30" Height="30"
                            Style="{StaticResource IconButtonStyle}" ToolTip="Cancel"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="Close_Click" />
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Margin="20,18,20,12">
                <TextBlock Text="{Binding DialogTitle}" FontSize="22" FontWeight="SemiBold" />
                <TextBlock Text="{Binding DialogDescription}" Foreground="{StaticResource TextSecondaryBrush}"
                           TextWrapping="Wrap" Margin="0,5,0,14" />
                <TextBox x:Name="SearchBox" ToolTip="Search by display name, type name, tag, description, or semantic similarity"
                         TextChanged="SearchBox_TextChanged" />
            </StackPanel>

            <Border Grid.Row="2" Margin="20,0,20,0" Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="5">
                <ListBox x:Name="TaskList" ItemsSource="{Binding FilteredTasks}"
                         SelectionChanged="TaskList_SelectionChanged" MouseDoubleClick="TaskList_MouseDoubleClick">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding DisplayName}" FontSize="15" FontWeight="SemiBold" />
                                <TextBlock Grid.Row="1" Text="{Binding TypeName}" Style="{StaticResource CaptionTextStyle}"
                                           Margin="0,3,0,0" />
                                <ItemsControl Grid.Row="2" ItemsSource="{Binding Tags}" Margin="0,6,0,0">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{StaticResource AccentSoftBrush}" CornerRadius="4" Padding="6,2" Margin="0,0,5,3">
                                                <TextBlock Text="{Binding}" FontSize="10" Foreground="{StaticResource AccentHoverBrush}" />
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock Grid.Row="3" Text="{Binding Comment}" Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0" MaxHeight="34" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <Border Grid.Row="3" Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0"
                    CornerRadius="0,0,8,8" Padding="20,12" Margin="0,16,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Double-click a type to create it" Foreground="{StaticResource TextMutedBrush}" />
                    <Button Grid.Column="1" Content="Cancel" IsCancel="True" MinWidth="82" />
                    <Button x:Name="CreateButton" Grid.Column="2" Content="Create Node" IsDefault="True"
                            IsEnabled="False" MinWidth="96" Style="{StaticResource PrimaryButtonStyle}"
                            Click="CreateButton_Click" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
