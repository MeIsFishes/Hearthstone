# 策划案实现方式建议章节 Skill 修改检查清单

- [x] 通过：确认修改对象与 skill 类型。更新既有底层 skill `.codex/private-skills/design-plan-doc/`，未新增或变更 agent 关联。
  - 证据：目标目录与 `name: design-plan-doc` 一致；`MAIN_AGENT.md` 中原有关联保持不变。
- [x] 通过：在策划案正文结构中加入可选章节“实现方式建议”。
  - 证据：`design-plan-doc/SKILL.md` 已定义 `## 8. 实现方式建议`。
- [x] 通过：明确该章只记录用户明确要求写入的实现方式建议，不主动调查、推断或补写；用户未要求时整章跳过。
  - 证据：第八章规则明确要求用户触发、未要求时不写标题或占位，并限制读取与主动扩写。
- [x] 通过：调整章节数量、顺序及写作流程中的相关表述，避免与“固定七章/不得增加章节”规则冲突。
  - 证据：正文结构改为“七个必选章节 + 可选第八章”；同步更新 `design-plan-writing.md` 和预检框架边界表述。
- [x] 通过：保持 skill 的 `name`、中文 `description`、路径和既有关联不变，并确认 description 简洁（中文不超过 40 字）。
  - 证据：`name` 仍为 `design-plan-doc`，description 长度 21；预检 skill description 长度 30。
- [x] 通过：检查条件性内容是否需要拆分文件；本次规则简短且属于正文结构核心约束，不创建额外说明文件。
  - 证据：新增规则仅两段，直接决定正文结构，保留在 `SKILL.md` 更利于整体一致性。
- [x] 通过：验证 skill YAML、名称、引用路径和 Markdown 结构有效。
  - 证据：等价校验确认两份 SKILL 均以合法 frontmatter 开始、仅含 `name`/`description`、目录名与合法 kebab-case 名称一致；官方 `quick_validate.py` 因环境缺少 `PyYAML` 未能运行，非内容校验失败。
- [x] 通过：检查未误改无关文件、未遗漏直接依赖、未创建或修改任何 `.meta` 文件。
  - 证据：功能修改限定为 `design-plan-doc/SKILL.md`、`project-state-preflight/SKILL.md` 与其 `design-plan-writing.md`；目标目录 `.meta` 搜索无结果。
- [x] 通过：框架边界审计。确认修改沿用既有策划案 skill、状态流程、归档和媒体规范，没有建立平行规则或绕过既有流程。
  - 证据：可选章追加在既有七章之后；第 6、7 章编号、状态流转、Plan/Review、归档与媒体规则均未改变。
- [x] 不适用：评估并同步直接相关文档。
  - 证据：本次仅修改策划案写作规范，不改变游戏当前实现、程序、美术或玩家视角设计现状，无对应正式现状文档需要同步。
- [x] 通过：任务结束前逐项复核并记录证据。
  - 证据：已重新读取本清单、三份修改文件、校验输出与清理脚本，并逐项记录审计结论。
- [x] 通过：只运行一次 `AutoDoc/CleanupTempDocs.bat`，记录结果，并在清理后创建同名 `*-Report.md`。
  - 证据：清理前后均为 2 份临时 Markdown，脚本退出码 0；因数量未超过 500，未删除文件。
