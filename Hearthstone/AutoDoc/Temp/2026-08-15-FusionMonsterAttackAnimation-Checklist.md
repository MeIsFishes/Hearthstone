# 融合怪攻击动画与多音效改造检查清单

- [x] （通过）盘点卡片列表中的全部基础怪与融合怪，确认原画、现有攻击帧、配置和运行时代码的对应关系。证据：确认 5 种基础类型、100～148 共 49 种独立融合表现、149～213 共 65 种四卡复用项。
- [x] （通过）将“闪红延迟”由单值改为列表，并确保多次命中按列表播放且序列化兼容范围清晰。证据：`HitDelays` 数组、运行时下一索引与 UI 多脉冲取最大强度已存在；旧测试/自定义表缺列时回落空数组。
- [x] （通过）将“音效”与“音效延迟”改为列表，支持单次攻击播放多个音效。证据：`AttackAudioKeys`、`AttackAudioDelays` 由会话按时序逐项消费。
- [x] （通过）新增“音量”列表，并定义长度不一致、空值和默认音量时的安全行为。证据：`AttackAudioVolumes` 与键、延迟强制等长，音量限制在 0～1；缺列回落空数组，不播放不完整项。
- [x] （通过）更新所有相关配置、编辑器展示、导出/加载链路和调用方，避免绕过既有数据与资源框架。证据：CSV、`BattleCardTypeCsvData`、会话、规则、System、卡牌 Controller 与编辑器测试统一使用数组字段。
- [x] （通过）描述每个基础怪当前的攻击动画；描述必须以现有原画和攻击帧为证据。证据：美术与玩家设计文档已分别记录剑痕、箭矢、小爆炸、小击打和大击打的八帧动作。
- [x] （通过）对每个融合怪，在绘制前查看其原画和对应基础怪攻击动画描述。证据：按卡号逐项以原画兵器、元素、单位数、基础动作与等阶确定提示词和命中结构。
- [x] （通过）为有独立融合原画的 100～148 号融合怪逐一绘制攻击帧动画，保持原画辨识度，并与基础怪攻击动作有明确区分。证据：49 个 `BattleAttackFusion_100.png`～`BattleAttackFusion_148.png` 均存在。
- [x] （通过）按用户补充要求，让 149～213 号四卡怪复用其排序配方去掉末项后所对应的三卡怪整套攻击表现（帧动画、音效、音量、闪红时点），不再重复绘制。证据：65 行全部引用三卡动画键，且四个列表字段与引用源逐字段一致。
- [x] （通过）对原画中包含多个单位的融合怪逐一判断是否需要多次命中，并让帧动画、闪红时序和音效时序一致。证据：配置使用 1～3 个命中点；共同操作单一重炮的 145 保留一次重命中，多单位/复合武器按画面使用两至三段反馈。
- [x] （通过）逐个检查新攻击帧的透明背景、尺寸、命名、切片/导入设置、帧序、锚点和资源引用。证据：49 张均为 1536×1024、Format32bppArgb、角点 Alpha 0/1、透明采样比例 0.723～0.967；文件名与配置键一致，现有 Texture 导入和 4×2 RawImage UV 播放链路匹配。
- [x] （通过）验证基础怪与融合怪的攻击播放、多个闪红节点、多个音效、延迟和音量行为；除非用户另有要求，不进入游戏运行验证。证据：CSV 静态验证 120 行、资源引用和列表时序无错误；主项目及测试项目编译通过。按项目默认未进入 Unity 游戏运行。
- [x] （通过）检查新增函数与字段是否确有复用价值，删除不必要的一次性抽象。证据：数组解析、延迟末项计算与运行时消费均由所有基础/融合类型共用；拼图脚本仅为临时美术工具并在结束清理。
- [x] （通过）框架边界审计：确认实现使用项目现有公开 API、生命周期、数据配置、对象池和资产导入流程，不保留平行实现或手写导出旁路。证据：继续使用 `DataApi`、`AudioApi`、现有会话生命周期与池化卡牌条目；最终资源走既有 Resources 路径。
- [x] （通过）误改与依赖审计：保留用户已有改动，不修改 `.meta`，核对无关文件及直接依赖。证据：攻击图集 `.meta` 无差异；未回退或覆盖工作树中其他任务文件。
- [x] （通过）玩家视角设计文档：完整读取基础格式 skill，核对玩家可见攻击表现变化并同步或记录不适用证据。证据：已按战斗系统特殊格式更新 `AutoDoc/Design/Specific/combat-system/combat-system.md`。
- [x] （通过）美术文档：完整读取基础格式 skill，记录全部基础怪攻击动画描述与融合怪攻击帧规范、路径和现状证据。证据：已按模块美术格式更新 `AutoDoc/Art/Modules/battle-card/battle-card.md`。
- [x] （通过）程序文档：完整读取基础格式 skill，记录多闪红、多音效、延迟、音量列表的数据与运行时现状。证据：已按特殊战斗与 UI 文档格式更新两篇程序文档。
- [x] （通过）执行静态检查/可用测试并记录结果；如无法运行 Unity，明确验证边界与剩余风险。证据：两项 `dotnet build --no-restore` 均 0 错误；`git diff --check` 通过；仅有现存程序集版本与换行提示。
- [x] （通过）结束前重新打开本清单，逐项标记通过、未通过或不适用，并附实际证据。证据：本次结束审计已逐项完成。
- [x] （通过）仅运行一次 `AutoDoc/CleanupTempDocs.bat`，记录退出结果；随后创建对应 `*-Report.md`。证据：批处理仅执行一次并返回退出码 0；报告在该次执行之后创建。
