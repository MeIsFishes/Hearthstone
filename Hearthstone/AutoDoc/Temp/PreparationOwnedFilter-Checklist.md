# 备战卡池“查看拥有”筛选检查清单

- [通过] 在备战选卡区域左上角增加默认未勾选的“查看拥有”勾选框，文字清晰且不遮挡卡牌、滚动条或拖放区域。
- [通过] 未勾选时保持完整 `01~148` 卡池；勾选时只显示当前运行中已拥有的卡牌。
- [通过] 筛选结果严格按卡牌编号升序排列，已拥有普通卡和融合卡使用同一规则，99 号封印位不作为拥有卡显示。
- [通过] 筛选切换后正确重建同一个 `UiList`、更新 Content 高度和滚动位置，不创建第二套卡池或绕过对象池。
- [通过] 拖放、融合、出战变化或拥有状态更新后，筛选模式中的显示结果与当前 `RunState` 保持一致。
- [通过] 在 View 中只保存 Toggle 引用，Controller 负责监听和筛选状态，玩法权威数据仍由现有 Run/Preparation Component 提供。
- [通过] 同步修改 `PreparationViewUiBuilder` 与 `PreparationView.prefab`，通过允许的 Unity Editor 流程落地，不手写 Prefab/Asset。
- [通过] 保持 BbxCommon View/Controller、`UiList`、对象池和 Unity `ScrollRect` 框架边界，不增加平行 UI 或自建滚动实现。
- [通过] 检查修改范围、直接依赖和工作区已有改动，不误改无关文件或任何 `.meta`。
- [通过] 检查新增函数和字段是否必要，避免一次性抽象、重复刷新或不必要分配。
- [通过] 更新定向 Editor 测试，并完成编译、Prefab 结构、筛选逻辑和差异验证。
- [通过] 玩家视角设计文档：读取格式 skill，核对并同步“查看拥有”入口、默认状态和编号排序体验。
- [通过] 美术文档：读取格式 skill，核对并同步勾选框的布局、字体、色彩与状态表现。
- [通过] 程序文档：读取格式 skill，核对并同步 Toggle 监听、列表重建、Content 高度和数据刷新逻辑。
- [通过] 框架边界审计：确认未绕过公开 API、生命周期、UiBuilder 配置源、UiScene 或资源流程。
- [通过] 结束前逐项复核，运行一次 `AutoDoc/CleanupTempDocs.bat`，并生成对应任务报告。
