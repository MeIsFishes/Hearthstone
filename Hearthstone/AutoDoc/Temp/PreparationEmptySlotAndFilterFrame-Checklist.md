# 备战空槽与“查看拥有”底框修正检查清单

- [x] 定位截图中 4 号已持有卡上方残留空槽/位置偏移的真实原因，修复共享卡片状态切换或回池复位，不做仅针对 4 号位的特判。
  - 证据：`OnPreparationDragReturned` 原先把卡片局部坐标强制写为零；7 列布局的零点恰好对应中间第 4 列。现改为先刷新列表布局，再读取该卡实际槽位坐标并提交一次高优先级回位请求。
- [x] 已持有卡显示时只保留完整卡面，不叠加卡池空态、空槽底图或上一绑定残留。
  - 证据：备战状态切换继续由 `ApplyPreparationState` 互斥控制；实体卡隐藏 `PreparationEmptyState`，根 `CardBackground` 统一保持透明，空态由独立完整底图承担。
- [x] “查看拥有”四字与勾选框放入同一个清晰底图框中，保持卡池左上角位置和整行点击区域。
  - 证据：`OwnedOnlyToggle` 根 `Image` 引用 `PreparationTabIdle`，尺寸仍为 `240 x 42`，根图形仍是 Toggle 的点击目标；文字保持“查看拥有”。
- [x] 检查项目现有通用 UI/木纹素材；无合适素材时用 imagegen 生成项目位图，并保存到项目可加载路径。
  - 证据：检查后未找到同时满足浅灰木纹空槽与细长文字框比例的通用素材；使用 imagegen 生成并归一化为 `PreparationPoolEmptySlot.png` 与 `PreparationTabIdle.png`，复用既有路径和 GUID。
- [x] 空卡槽边框进一步变细，视觉上不再形成粗浅灰外圈。
  - 证据：新空槽素材使用极细银灰边；`PreparationEmptyState` 从四边内缩 5 改为全尺寸贴合，根背景透明，避免双层边缘叠成粗框。
- [x] 空卡槽内部使用浅灰木材质，保持编号清楚、内容区低对比，且不误导为已持有卡。
  - 证据：最终空槽纹理为低对比浅灰木板、稀疏木纹及透明外角；资源检查为 `1024 x 1536` RGBA。
- [x] 空槽与已持有卡仍保持 `25:36` 比例、7 列布局、滚轮转发与拖放命中规则。
  - 证据：未修改卡池网格列数、槽位比例、`ScrollRect` 或滚轮转发路径；实体卡拖放仍由共享卡片控制器处理，空槽继续不拦截射线。
- [x] 同步修改共享卡片 Builder、备战页面 Builder 与对应 Prefab；通过 Unity Editor/Builder 正常流程落地，不手写 Prefab YAML。
  - 证据：更新 `BattleCardItemUiBuilder` 与 `PreparationViewUiBuilder` 后，在已连接 Unity Editor 中执行两个 Builder 并保存资源；Prefab 结构读取验证通过。
- [x] 保持 BbxCommon View/Controller、共享 `BattleCardItemController`、`UiList`、对象池和 `ScrollRect` 框架边界，不新增平行卡片或滚动实现。
  - 证据：改动仅复用现有共享控制器、`UiTransformSetter`、三个 `UiList.RefreshLayout` 和现有 Toggle；未增加并行 UI 或滚动组件。
- [x] 检查工作区已有改动和直接依赖，不回退无关修改，不创建、编辑或删除任何 `.meta`。
  - 证据：保留工作区全部既有变更；任务资源复用已有文件路径，`git diff --name-only -- '*.meta'` 无本任务跟踪元文件改动。
- [x] 检查新增函数和字段是否必要，避免一次性抽象、重复刷新和不必要分配。
  - 证据：未新增长期字段或新类型；删除不再使用的空槽背景色常量，回位仅复用刷新后已有坐标和已有 `UiTransformSetter`。
- [x] 更新或补充定向 Editor 测试，完成 C# 编译、Prefab 结构、资源透明通道/尺寸、状态互斥和关联回归验证。
  - 证据：补充回位非零点、根背景透明、空态零内缩、文字框 Sprite/尺寸与资源透明通道断言；相关 EditMode 测试 51/51 通过，三个程序集均 0 error。
- [x] 玩家视角设计文档：完整读取格式 skill，按已实现现状同步 4 号残留修复、文字底框和浅灰木纹空槽体验。
  - 证据：已更新 `AutoDoc/Design/Specific/preparation-card-pool/preparation-card-pool.md`。
- [x] 美术文档：完整读取格式 skill，按已实现资源同步底框、细边与浅灰木纹规格。
  - 证据：已更新 `AutoDoc/Art/Modules/preparation-card-pool/preparation-card-pool.md`，记录尺寸、风格、透明区和路径。
- [x] 程序文档：完整读取格式 skill，按实际实现同步共享卡片状态互斥、Builder/Prefab 与资源引用。
  - 证据：已更新 `AutoDoc/Program/UI/preparation/preparation.md`，记录真实槽位回位、空态互斥和 Toggle 根图引用。
- [x] 框架边界审计：确认未绕过公开 API、生命周期、UiBuilder、预加载、UiScene 或资源流程。
  - 证据：资源通过 `Resources` 既有键加载，Prefab 通过既有 Editor Builder 重建；未修改预加载、UiScene 或底层资源生命周期。
- [x] 结束前逐项复核，按要求只运行一次 `AutoDoc/CleanupTempDocs.bat`，并生成对应任务报告。
  - 证据：本清单已逐项审计；清理脚本只运行一次并返回 0，随后生成 `PreparationEmptySlotAndFilterFrame-Report.md`。
