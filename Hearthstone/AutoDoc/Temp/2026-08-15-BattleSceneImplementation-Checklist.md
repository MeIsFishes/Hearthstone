# 战斗场景与五怪物卡实现检查清单

- [x] 确认第二版战斗场景与五怪物卡面原画已作为当前美术文档参考，并保持来源、路径和“已实现/概念参考”状态准确。
  - 证据：`AutoDoc/Art/Style/art-style-overview.md` 与 `AutoDoc/Art/Modules/battle-card/battle-card.md` 均保留 V2 概念图为主参考，并区分概念图与运行 Sprite。
- [x] 按项目规则恢复并验收唯一允许的 `unityMCP` 链路：包与 Server 固定 `v10.0.0`，当前会话可调用，完成活动场景与错误 Console 只读探针。
  - 证据：`com.coplaydev.unity-mcp` 与 `mcpforunityserver` 均固定 `10.0.0`；最终 `get_active` 返回 `Main` 且未脏，错误 Console 为 0。
- [x] 读取战斗代码、配置、UI Controller、Prefab/UiScene 结构及适用项目 skill，确认现有 ECS、DataApi、UI 生命周期、资源加载和导出边界。
  - 证据：已核对 `BattleRules`、`BattleStages`、两个战斗 Controller/View、CSV、`BattleView.prefab`、`BattleCardItem.prefab`、`Battle.asset` 及相关 skill。
- [x] 将当前战斗界面实现为第二版原画方向：正俯视、浅色简约战场底板、上下各三卡、中央战斗反馈区域、蓝红阵营与清晰攻击/目标反馈。
  - 证据：`BattleView.prefab` 的战场底板扩大为 `1500 × 900`，双方 UiList 位于上下两排，中央保留回合/结果区域；原攻击、目标覆盖层保留。
- [x] 建立五种怪物卡数据：哥布林战士、哥布林弓手、哥布林投弹手、野猪、食人魔；为名称、属性、技能说明和原画资源建立可维护配置，不在 UI 层硬编码业务数据。
  - 证据：`BattleCardCsvData.csv` 包含 ID 1–5、`DisplayName`、`ArtworkKey`、攻血与说明；Controller 通过 `DataApi` 读取。
- [x] 准备并导入五种独立怪物卡原画/Sprite，保持第二版明亮奇幻风格、人物约占主视觉三分之二；不得依赖横向概念同页直接作为单卡运行资源。
  - 证据：`Assets/Resources/Art/BattleCards/` 下五张 `1024 × 1536` 独立 PNG 已由 Unity 导入为单图 Sprite。
- [x] 调整战斗初始化，使双方三张卡从五种怪物配置中形成明确且可验证的阵容，并保持自动交替行动、随机目标、同时伤害与对象池/UI 列表生命周期。
  - 证据：`BattleRules.GetCardConfigId()` 提供我方 `[1,4,5]`、敌方 `[2,3,1]`；既有 `BattleSystem` 与 ECS/UI 生命周期未建立平行实现。
- [x] 调整卡牌 UI 显示怪物名称、原画、技能说明、生命与攻击，并保持死亡、高亮、池化换绑和正向显示状态正确。
  - 证据：`BattleCardItemController` 使用 `ResourceApi.LoadSprite()`，解绑时清空 Sprite/说明并恢复单位旋转；Prefab 继续引用死亡、攻击者、目标覆盖层。
- [x] 按用户追加要求重绘运行时卡面元素：独立卡面边框、红色剑形攻击底框、绿色血滴生命框；使用透明 Sprite，保持明亮奇幻、简约易读且不含文字。
  - 证据：`CardFrame-v2.png`、`AttackSwordBadge.png`、`HealthDropBadge.png` 已生成、实际查看并导入；三图角点 Alpha 均为 0，数字由 TMP 叠加。
- [x] 调整卡面构图，使怪物原画主体占据整个上半部分并明显增大，下半部分保留紧凑的名称/说明与属性徽章。
  - 证据：`ArtworkViewport` 覆盖顶部 66%，使用 `RectMask2D`；内部 `ArtworkArea` 为 `208 × 312`，在 `208 × 199.2` 视窗内保持 2:3 比例并放大裁切；说明区位于底部 34%。
- [x] 通过 Unity MCP 修改/导出 Unity 资产，不手写 Scene、Prefab 或 `.asset` YAML；不创建、编辑或删除任何 `.meta` 文件。
  - 证据：Prefab 层级、组件、Sprite Importer 与保存均通过 `unityMCP` 完成；`.meta` 仅由 Unity 自动生成。
- [x] 验证源码编译、配置加载、Prefab 引用、Sprite 导入、五怪物资源与三对三静态布局、Console 新增错误；用户已明确本轮不用进入游戏测试，因此 Play Mode、战斗运行状态与对象回收验证标记不适用。
  - 证据：源码刷新后 0 编译错误，完整 EditMode 测试 13/13 通过；最终 Prefab 静态检查为 14 个对象且三个 UI Sprite 引用有效，Console 0 错误。按用户要求未在本轮进入 Play Mode。
- [x] 框架边界审计：确认使用 DataApi、ResourceApi/BbxCommon UI、ECS 生命周期、UiScene/Prefab 导出和项目对象池，没有平行管理器、直接 Resources 绕行或手写导出产物。
  - 证据：配置经 DataApi，原画经 ResourceApi，条目继续由 UiList/Controller 生命周期管理；本次只改 View Prefab 内部，不涉及 Group、默认显隐、整体位置或导出字段，因此无需重导 UiScene。
- [x] 检查新增函数、字段和配置结构是否具有复用价值，删除不必要的一次性抽象；检查未误改无关文件和直接依赖。
  - 证据：仅新增通用配置字段与阵容查询，卡面装饰使用 Prefab 静态层级；未新增管理器或一次性运行时构建。工作目录未包含 `.git`，无法使用 Git diff，已按本任务实际操作路径逐项核对。
- [x] 玩家视角设计文档：完整读取基础与战斗专项格式，按实际玩家可见结果更新战斗系统文档并记录验证证据。
  - 证据：已读取 `design-doc-format` 与战斗专项格式，更新 `AutoDoc/Design/Specific/combat-system/combat-system.md` 的五怪物阵容、数值和卡面视觉。
- [x] 美术文档：完整读取基础与适用格式，确保第二版原画作为参考，并按实际运行资产、规格和路径更新美术文档。
  - 证据：已读取 `art-doc-writer`、`generate-art-assets` 及风格/UI/模块格式，更新风格总文档、UI 美术文档和战斗卡模块文档。
- [x] 程序文档：完整读取基础、战斗系统和 UI 界面格式，按实际代码、配置、资源与调用链更新程序文档。
  - 证据：已读取 `program-doc-format`、战斗专项与 UI 界面格式，更新战斗系统程序文档和战斗界面程序文档。
- [x] 结束前重新读取本清单，逐项标记通过、未通过或不适用并记录证据；仅运行一次 `AutoDoc/CleanupTempDocs.bat`，随后创建同任务名报告。
  - 证据：本次已完成逐项复核；清理脚本与报告作为紧随其后的最终动作执行。
