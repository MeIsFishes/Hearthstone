# Unity MCP 与 UiScene Skill 更新检查清单

- [x] **通过：修改对象是既有底层 skill。**
  - 证据：修改位于 `.codex/private-skills/recover-unity-mcp/` 与 `.codex/private-skills/bbxcommon-ui/`；未创建平行 skill，未改变主代理索引或其他 agent 关联。
- [x] **通过：工具不可用分层排查完整。**
  - 证据：`recover-unity-mcp/SKILL.md` 新增“工具不可用排查”，依次区分完整工具目录、延迟工具实调用、MCP 注册、Server 握手/tools/list、Editor 实例和调用错误分类。
- [x] **通过：覆盖 `functions.exec` / `ALL_TOOLS` 延迟工具。**
  - 证据：明确不能把顶层展示等同完整目录；环境提供 `ALL_TOOLS` 时先筛选 `mcp__unityMCP__*`，读取实际 schema、MCP resources、instances/editor state，再执行 `manage_scene(get_active)` 与 `read_console(error)`。
- [x] **通过：故障矩阵同步。**
  - 证据：`references/failure-matrix.md` 的 A 分支把“完整目录无直接或延迟工具”设为进入条件；发现延迟工具且只读调用成功时立即退出缺失分支，不重载或重装。
- [x] **通过：UiScene 创建流程完整。**
  - 证据：`bbxcommon-ui/SKILL.md` §2.4 分为准备代码与 View Prefab、创建 UI 编辑场景、导出与运行时接入、验收与收尾四段，覆盖用户要求的全部环节。
- [x] **通过：Editor 与自动化边界明确。**
  - 证据：要求保护并恢复原活动场景，Scene/Prefab/Asset 只能由项目允许的 Unity MCP/Editor API 创建保存，UI 编辑场景为配置源，禁止文件工具手写 Unity YAML 或直接写 `UiObjectDatas`。
- [x] **通过：frontmatter 与引用有效。**
  - 证据：name 分别为 `recover-unity-mcp`、`bbxcommon-ui`；description 长度分别为 32、40 个字符，符合不超过 40 的要求；正文为中文，检查到的全部相对 Markdown 引用均存在。
- [x] **通过：条件性内容拆分合理。**
  - 证据：所有任务必需的排查和创建步骤保留在主 `SKILL.md`；具体故障分支继续位于既有 failure matrix，完整导出细节继续位于既有 `ui-scene-export.md`；未新增辅助文档。
- [x] **通过：修改范围与框架边界。**
  - 证据：仅修改两个底层 skill 的主文档、Unity MCP 既有故障矩阵和本任务文档；未读取或修改 `AutoDoc/DesignPlan/`，未触碰 Unity 资产、源码或 `.meta`，没有建立平行流程。
- [x] **不适用：正式程序、美术或玩家视角文档同步。**
  - 证据：本次只完善代理操作规范，不改变游戏当前实现、玩家表现或美术资产，无正式现状文档需要同步。
- [x] **通过：结束复核。**
  - 证据：已重新读取两个主 skill 的新增章节、故障矩阵 A 分支及后续相邻章节；必需关键词、步骤顺序、description 和引用检查均通过。
- [x] **通过：收尾动作已就绪。**
  - 证据：下一步只执行一次 `AutoDoc/CleanupTempDocs.bat`，随后创建独立报告；不在执行过程中重复清理。
