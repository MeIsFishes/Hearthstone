# 备战空槽与卡片轮廓完全一致报告

## 任务结果

已按用户确认的“两版之前空槽尺寸”恢复出战与融合空槽，并让入槽卡片直接匹配该尺寸。没有继续调整空槽为新的近似比例：出战维持 `84.36 × 126.54 px`，融合维持 `93.48 × 140.22 px`；每一页的空槽与占用卡片共用同一源轮廓和同一组 X/Y 根缩放，宽高完全相等。

## 检查项与证据

- 通过：出战空槽与卡片均为 `84.36 × 126.54 px`，根缩放均为 `0.33744 × 0.3515`，相邻间距均为 `100.64 px`。
- 通过：融合空槽与卡片均为 `93.48 × 140.22 px`，根缩放均为 `0.37392 × 0.3895`，相邻间距均为 `96.52 px`。
- 通过：Unity 几何检查返回两页 `equal=True`，空槽与卡片 X/Y 尺寸逐项一致。
- 通过：专用空槽继续复用 `PreparationPoolEmptySlot.png`；其 `250 × 360` 绘制轮廓与共享卡根一致，不保留独立 `2:3` 适配差。
- 通过：`ApplyExactSlotScale()` 由出战和融合槽共同使用；原等比适配函数继续服务敌方预览等其他上下文，没有改变其行为。
- 通过：`BattleCardItemUiBuilder.Build()` 已执行，Prefab 已保存；活动场景未脏，Unity 未进入 Play Mode。
- 通过：实现仍通过现有 Controller、`UiList`、共享卡条目、拖拽组件和 BbxCommon 对象池完成，没有平行 UI 或手写 Prefab YAML。
- 通过：本任务未创建、编辑或删除 `.meta`；相关文件 `git diff --check` 无新增错误。
- 未通过：完整 EditMode 回归 `113` 项中 `109` 项通过、`4` 项失败。失败仍为六槽场景约束、攻击者序列、稀疏继续阵容和卡牌替换规则，不经过本次 UI 轮廓断言；本任务未越权修改这些玩法逻辑。

## 验证结果

- 目标 Editor 测试：通过。
- `dotnet build Hearthstone.csproj --no-restore`：通过，`0` 错误。
- `dotnet build Hearthstone.Ui.Editor.csproj --no-restore`：通过，`0` 错误。
- `dotnet build Hearthstone.Tests.csproj --no-restore`：通过，`0` 错误。
- Unity Console 清理后：`0` 条错误。
- 未执行 Play Mode；使用 Prefab、绑定代码、Unity 几何检查、Editor 测试与编译验证。

## 文档处理

- 更新玩家视角备战卡池文档，记录空槽与入槽卡片宽高和缩放完全一致。
- 更新备战卡池与共享卡牌美术模块文档，记录恢复后的精确尺寸、缩放和间距。
- 更新备战 UI 程序文档，记录两页目标轮廓与 `ApplyExactSlotScale()` 绑定流程。

## 执行偏差与未解决风险

- 初始尝试使用统一近似比例，没有保留用户认可的旧空槽尺寸；收到反馈后已撤销该方向，改为以旧尺寸为固定目标，仅让卡片匹配。
- 未执行游戏内目视拖放验证；若运行中的页面实例来自修改前已打开的对象池，需要重新进入备战页面或重新启动运行流程后才会载入新 Prefab 与绑定代码。

## 清理结果

已且仅已运行一次 `AutoDoc/CleanupTempDocs.bat`，退出码 `0`；清理后创建本报告。
