# 备战页敌方阵容预览抽屉检查清单

- [x] 通过 — 备战出战页新增窄矩形旧木侧签与独立右箭头；Prefab 与 Builder 均存在序列化引用。
- [x] 通过 — 点击后以 `0.34 s` 位移和淡入拉开木制面板，横排展示下一场敌方卡牌列表。
- [x] 通过 — 再次点击播放反向位移和淡出；展开过程中不旋转箭头，完全展开后切左，完全收起后切右。
- [x] 通过 — 最终侧签为严格窄矩形，延续上一版粗糙旧木、板缝结疤和磨损暗古铜薄边，不使用新漆、花边或卡通装饰。
- [x] 通过 — 箭头只包含纯三角箭头头部，不含矩形箭杆；居中 Rect 为 `66 × 72`，补偿源图透明安全区。
- [x] 通过 — 收起时侧签可见边缘贴住屏幕左侧；展开时木板左边缘贴屏，侧签 Rect 向木板内重叠 `14 px`，透明安全边也有实际覆盖。
- [x] 通过 — 侧签根在出战页始终显示；旧 Session 无敌方快照时只禁用点击，不再隐藏整个侧签。
- [x] 通过 — 使用 `imagegen` skill 补齐面板、矩形旧木侧签与箭头；矩形以先前最终梯形为材质参考，只改变轮廓并恢复真实透明 Alpha，无文字、水印或多余装饰。
- [x] 通过 — 三张最终 PNG 均已目视检查；尺寸为 `2172×724`、`650×1628`、`1237×1272`，四角 Alpha 均为 0，路径位于 `Assets/Resources/Art/Preparation/UI/`。
- [x] 通过 — 静态层级与引用由 `PreparationViewUiBuilder.Build()` 写入 `PreparationView.prefab`，View 只保存引用/位置，Controller 负责数据与动效。
- [x] 通过 — 敌方预览使用 `UiList.ConstantSlot/Horizontal` 和现有 `BattleCardItemController` 对象池条目，没有平行列表或普通 GameObject 刷新创建。
- [x] 通过 — `EnemyBattlePreviewStartupData` 在进入备战前固定完整实例，Preparation session 与 Battle startup 防御性复制同一快照，战斗不二次随机。
- [x] 通过 — 基础卡和融合卡均经 `BattleCardSimulationFactory` 生成；预览绑定实例的攻血、词条、等阶和表现编号，战斗消费相同实例。
- [x] 通过 — 打开、关闭、隐藏、重入和页签切换均复位抽屉；列表随 UI 生命周期回池，预览快照随 Preparation session 回收。
- [x] 通过 — 开合使用可逆 `MoveTowards` 与 `SmoothStep`，快速反向从当前进度继续；箭头只在端点切换，不会运动中旋转。
- [x] 通过 — 参考分辨率 `1920×1080` 的关闭态与展开态编辑器渲染确认侧签、标题和六卡显示；最多 6 张卡以 `185 px` 步长容纳于 `1110 px` 列表。
- [x] 通过 — `ResourceApi.LoadSprite()` 可读取三个唯一 key；自动资源字典中每个 key 各出现一次。
- [x] 通过 — 已通过 Unity 内执行 Builder 重建 Prefab，没有手写 Prefab、场景或导出 Asset YAML。
- [x] 通过 — 数据快照、列表绑定、抽屉状态和布局职责分离；未发现重复的一次性生成抽象。
- [x] 通过 — 新增敌方快照一致性和 Prefab/资源/布局/端点箭头测试；最新全量 EditMode `114/114` 通过。
- [x] 通过 — 未绕过 View/Controller、UiList、DataApi、ResourceApi、GameStage/ECS 生命周期、Builder 或资源导出流程。
- [x] 通过 — 保留现有脏工作树和无关改动；三张新 PNG 的 `.meta` 由已打开 Unity 自动导入生成，未手工创建或修改。
- [x] 通过 — 已读取 `design-doc-format` 并更新玩家视角备战编成现状文档。
- [x] 通过 — 已读取 `art-doc-writer` 与模块格式，更新最终资产、尺寸、透明、贴边、无缝及箭头规格。
- [x] 通过 — 已读取 `program-doc-format` 与 UI 界面格式，更新快照链路、View/Controller/Builder、列表与生命周期现状。
- [x] 通过 — `Hearthstone.Tests.csproj` 编译 0 error（保留既有程序集版本冲突 warning）；Unity Console 编译 0 error；静态资源、Prefab 和全量 EditMode 测试通过。按默认未主动进入游戏验证。
- [x] 通过 — 已重新打开清单逐项核对并修复侧签运行态隐藏、箭头可见尺寸、矩形材质、接缝重叠与 4 个过期测试前置条件。
- [x] 通过 — 结束审计后已且仅已运行一次 `AutoDoc/CleanupTempDocs.bat`，并创建 `EnemyPreviewDrawer-Report.md`；用户后续视觉修订只更新同一清单和报告，未重复清理。
- [x] 通过 — 展开按钮相对面板中心上移 `20 px`，并保持关闭态左侧贴屏和展开态紧贴面板；Prefab 断言与静态渲染通过。
- [x] 通过 — 展开面板改为 `1500 × 400`，左侧新增“敌方阵容”标题，`1110 × 266.4` 列表完整容纳最多六张卡且不裁切。
- [x] 通过 — 敌方预览卡以共享卡根 `250 × 360` 为缩放基准，最终呈现尺寸与我方出战卡一致为 `150 × 216`、根缩放 `0.6`；代码不再使用列表 Controller 外层尺寸误算。
- [x] 通过 — 新布局已更新 Builder、Prefab、自动测试和玩家视角/美术/程序文档；针对性 `2/2`、全量 EditMode `114/114`、测试程序集编译 `0 error`。
