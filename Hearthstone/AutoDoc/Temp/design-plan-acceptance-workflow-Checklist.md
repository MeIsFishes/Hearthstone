# 策划案验收结构与实现流程修改检查清单

- [x] 通过：确认修改对象与 skill 类型。更新既有底层 `design-plan-doc`、`project-state-preflight` 及直接关联的两个策划案审查 agent，未新增关联或平行流程。
  - 证据：修改限定在两套既有底层 skill 及 `.codex/agents/` 下原有 Plan/代码审查 agent。
- [x] 通过：将原两个验收二级章合并为一个 `## 6. 验收`，包含三个固定三级小节。
  - 证据：`design-plan-doc/SKILL.md` 明确定义 `### 6.1 验收方式`、`### 6.2 美术资产验收`、`### 6.3 程序功能验收`。
- [x] 通过：定义“流程log验收”“游戏内截图”“仅功能验收”三种方式，并将流程log验收设为默认。
  - 证据：策划案 skill、写作流程、实现流程和两个审查 agent 均使用相同三个名称与默认规则。
- [x] 通过：保留 `ART-` 与 `FUNC-` 稳定编号、分类验收与跨分类关联边界。
  - 证据：第 6.2、6.3 节分别维护编号；实现和 Review 规则禁止跨类别混合 case 或互相替代结论。
- [x] 通过：将可选“实现方式建议”顺延为第 7 章，并保持条件出现。
  - 证据：正文结构为六个必选章加可选第七章；未要求时仍跳过标题与占位。
- [x] 通过：更新策划案写作检查项、章节数量及框架边界表述，消除旧第 6/7 章引用。
  - 证据：全量检索未发现旧 `## 6. 美术资产验收`、`## 7. 程序功能验收`、七个必选章或可选第八章引用。
- [x] 通过：重构实现策划案的验收阶段。
  - 证据：步骤 e、f 明确五项顺序门槛，并分别细化并行进度、合并判断、逐案列表、统一趟次和正式验收/Review 交接。
- [x] 通过：合并验收保留完整映射和逐案结论。
  - 证据：强制记录“验收趟次 → 策划案 → case → 原 ART-/FUNC- 编号”；合并执行仍逐篇、逐类别判定。
- [x] 通过：同步 Plan 审查与代码/最终 Review agent。
  - 证据：两个 TOML 均接收第 6.1/6.2/6.3 节；最终 Review 第 2 章包含验收方式、美术资产验收、程序功能验收三个小节。
- [x] 通过：复核两个既有底层 agent 元数据、权限、输出和关联。
  - 证据：两个 `name` 合法，description 分别为 24、23 字，TOML 语法通过，skill 引用均存在，主代理注册表均保留；职责、描述、路径和类型未变化，无需改注册信息。
- [x] 通过：将策划案执行完毕的 Git 门槛设为 `git status --short` 无输出。
  - 证据：逐篇提交后记录状态，批次提交后和最终报告后均要求无输出；任务外遗留改动明确禁止擅自提交、修改或丢弃，只能阻塞上报。
- [x] 通过：保持 skill `name`、中文 `description`、路径和既有关联不变。
  - 证据：`design-plan-doc` 与 `project-state-preflight` 名称/目录一致，description 长度分别为 21、30，均不超过 40。
- [x] 通过：条件性内容不拆分文件。
  - 证据：新增内容直接约束既有正文结构、实施步骤和 agent 输入契约，均属于对应文件每次使用必须整体掌握的核心流程。
- [x] 通过：验证 SKILL frontmatter、名称、Markdown 结构、TOML 语法和新增引用路径。
  - 证据：两份 SKILL 等价校验均通过；两个 TOML 由 Python `tomllib` 解析通过；所有 agent skill 路径存在。官方 `quick_validate.py` 因环境缺少 `PyYAML` 未能启动，非内容校验失败。
- [x] 通过：检查未误改无关文件、未遗漏直接依赖、未创建或修改 `.meta` 文件。
  - 证据：功能修改限定为 6 个直接相关规则/agent 文件；目标目录 `.meta` 检索数量为 0。当前目录不是 Git worktree，无法使用 Git diff，已改用明确路径与旧规则全量检索审计。
- [x] 通过：框架边界审计。
  - 证据：继续沿用既有策划案、Plan、Review、两个注册 agent、媒体归档和 Git 提交流程；只重组验收契约，没有建立平行体系。
- [x] 不适用：正式程序、美术和玩家视角设计文档同步。
  - 证据：本次只修改工作流规范与 agent 提示词，不改变游戏当前实现或玩家可见内容。
- [x] 通过：任务结束前重新读取清单并逐项记录证据。
  - 证据：已复核 6 个修改文件、规则检索、frontmatter、TOML、引用路径、agent 注册和 Git 环境结果。
- [x] 通过：只运行一次 `AutoDoc/CleanupTempDocs.bat`，记录结果，并在清理后创建同名 `*-Report.md`。
  - 证据：清理前后均为 7 份临时 Markdown，脚本退出码 0；数量未超过 500，未删除文件。
