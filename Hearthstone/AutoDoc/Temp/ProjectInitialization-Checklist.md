# 项目初始化检查清单

- [通过] 通过 Unity 根标记、业务代码与关键入口证据判定项目为完全空白、只有框架、近空、已形成或不确定状态。
  - 证据：初始化前扫描为 Unity 工程、业务脚本 0、Scene 0、无 GameEngine/Stage/ECS/UI 信号，人工判定为“只有框架的空项目”。
- [通过] 明确项目名、C# 根 namespace、业务代码根目录和默认占位范围，保留全部已有用户资产。
  - 证据：`ProjectSettings.asset` 的 `productName` 为 `Hearthstone`；采用 `Hearthstone` namespace 与 `Assets/Scripts/Hearthstone/`，默认保留 ECS、配置和 MVC/UI 占位范围。
- [通过] 通读 `.codex/agents/` 与 `.codex/project-files/agents/` 下全部 subagent TOML，逐项核验项目级 skill/extension 关联，仅移除结构无效项并记录原因。
  - 证据：读取 5 份 `.codex/agents/*.toml`；项目 agent 目录不存在。所有 skill 路径存在，`design-doc-writer-agent-extension.md` 及其 `unit-design-docs` skill 均存在，未发现或移除无效关联。
- [通过] 先 dry-run 再补齐 BbxCommon 基线 Unity 包；不覆盖版本冲突，不编辑 `packages-lock.json`。
  - 证据：dry-run 与实际执行均显示 Entities `1.0.0-pre.65`、TMP `3.0.6`、UGUI `1.0.0` 已存在，新增包为 0；未编辑 `packages-lock.json`。
- [通过] 核实 BbxCommon、CrossLibrary、UniTask、Odin Inspector 及关键公开 API 的存在性；框架缺失或不可用时停止模板实例化。
  - 证据：四项框架/资产路径均存在；源码确认 `GameEngineBase`、`GameStage`、`EcsApi`、MVC 和 `SetActiveGameStage` 等 API 存在。Unity 编译状态另见未通过项。
- [通过] 若属于空或近空项目，按现有命名和结构增量创建默认占位 asmdef、GameEngine、GameStage、配置、ECS Component/System 与 MVC/UI 代码，不覆盖、不移动、不重命名已有文件。
  - 证据：模板 dry-run 复查 9 个标准文件全部为 `unchanged`，无冲突；另创建独立 Editor asmdef 与入口类型。
- [通过] 确保最小流程为入口 → 具名初始 Stage Group → Stage → Component → System，Group 入口由 GameEngine 实际调用并使用一次 `SetActiveGameStage(...)`。
  - 证据：静态调用链为 `HearthstoneGameEngine.OnAwake` → `EnterInitialStageGroup` → `PlaceholderStages.CreateBaseStage` → `InitializePlaceholderState`/`PlaceholderStateSystem`，且入口调用一次 `SetActiveGameStage(m_BaseStage)`。
- [未通过] 为现有 Game Stage Editor 提供无输入初始 Group 入口类型；入口资产必须由 Unity 创建到 `Assets/Resources/Editor/`，不得手写 YAML。
  - 证据：`InitialStageEntryAsset.cs` 已创建；`Assets/Resources/Editor/InitialStageEntry.asset` 尚未生成。目标 Unity 被当前 Package Manager 错误窗口阻塞，未手写资产。
- [通过] 核对业务 asmdef 的全部直接依赖、System 注册与顺序、Component 创建/回收、Stage Load/Unload、View/Controller 与 ECS 状态监听。
  - 证据：两个 asmdef 均可解析为 JSON；Runtime 引用 BbxCommon、CrossLibrary、Entities、Collections、TMP，Editor 另直接引用 BbxCommon、Runtime、TMP、UGUI；创建/移除单例、System 注册与顺序、监听绑定均有静态证据。
- [通过] 不创建或编辑 `.meta`、Scene/Prefab YAML、`.asset` YAML、`packages-lock.json`、资源索引以及 Unity/IDE/BbxCommon 自动生成文件。
  - 证据：当前业务目录无 `.meta`，目标 Scene/Prefab/Asset 均不存在；仅准备调用 Unity Editor API 的一次性构建器，没有手写 YAML 或生成产物。
- [通过] 框架边界审计：不绕过 BbxCommon 公开 API、GameStage、ECS/MVC 生命周期、Editor 配置源与导出流程，不保留平行实现或手写导出产物。
  - 证据：业务代码使用 StageWrapper、EcsApi、ModelWrapper、UiSceneExporter、GameStageWindow 与 Unity Editor API；未访问框架内部管理器，未建立第二套生命周期或资源导出流程。
- [通过] 代码质量审计：不引入无复用价值的一次性抽象、无需求的 Manager/Service/Repository 或重复业务类型。
  - 证据：Runtime 仅含标准 Placeholder 类型；Editor 构建器是初始化 skill 明确允许的一次性资产构建工具，成功后应删除，目前因阻塞暂留以便自动完成。
- [通过] 检查并同步与初始化现状直接相关的程序、玩家视角设计或美术文档；不读取或改写无关策划案。
  - 证据：创建 `AutoDoc/ProjectOverview.md`，记录实际 namespace 与当前骨架状态；尚无真实玩家功能和美术资产，普通 Design/Art 文档不适用；未读取策划案目录。
- [未通过] 执行静态复查、初始化前后扫描和可用的目标 Unity 编译/PlayMode 验证；无法执行的 Editor 操作形成具体待办且不宣称完成。
  - 证据：模板复查与初始化后扫描通过；匹配版本 Unity `2022.3.62f3c1` 已找到，但当前已打开的 Editor 在既有 `com.boxqkrtm.ide.cursor` Git 包首次解析失败后停在 Package Manager 窗口，批处理实例又因项目锁被拒绝，未完成编译与 PlayMode。
- [通过] 列明自动添加的包、自动生成文件边界、Editor 待办、全部 `Placeholder*` 的未来替换方向、偏差与风险。
  - 证据：将在同名任务报告中分别记录包结果、生成边界、待创建资产、替换方向与 Package Manager 阻塞。
- [通过] 复核实际改动仅覆盖任务相关文件且未误改用户已有内容。
  - 证据：初始化前无业务根目录与项目总览；所有新增内容位于 `Assets/Scripts/Hearthstone/`、`AutoDoc/ProjectOverview.md` 与任务临时文档。仓库未接入 Git，无法提供版本基线 diff；`manifest.json` 语义与最终排列已恢复原状。
- [通过] 结束前逐项写入通过/未通过/不适用及证据，只运行一次 `AutoDoc/CleanupTempDocs.bat`，随后生成同名 `*-Report.md`。
  - 证据：检查项已逐项复核；清理脚本仅执行一次并返回 `0`；已创建 `AutoDoc/Temp/ProjectInitialization-Report.md`。
