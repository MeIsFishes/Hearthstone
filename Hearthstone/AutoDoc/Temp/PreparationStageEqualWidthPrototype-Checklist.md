# 备战阶段等宽卡位原型修正检查清单

- [x] 需求修正：不再使用生成式图像近似卡位尺寸，改用确定性矢量栅格绘制。
  - 证据：新增 `preparation-stage-ui-prototype-v4.svg`，所有布局由明确数值和复用符号定义。
- [x] 卡位几何：14 个卡位复用同一固定宽高组件，严格为 7 列×2 排，列距和行距固定。
  - 证据：SVG 元数据为 `160×240`、列步进 `190`、行坐标 `410/665`；解析结果为 10 个空槽加 4 个已拥有槽，共 14 个。
- [x] 状态一致：已拥有卡与空槽共用完全相同的外轮廓尺寸，只改变内部内容。
  - 证据：空槽 `<use>` 与已拥有卡外层 `<rect>` 均使用 `160×240`，统一列坐标为 `100,290,480,670,860,1050,1240`。
- [x] 页面内容：保留上方恰好 3 个战斗槽位、下方 `1~98` 可滚动卡池和“本轮获得 5 张卡”反馈。
  - 证据：PNG 人工检查可见两个已占用战斗槽、一个空战斗槽、滚动条和发牌反馈。
- [x] 完整卡面：已拥有卡保留完整竖向卡面的立绘、名称、攻击、血量和灰色六边形编号区域。
  - 证据：`01`、`04`、`07`、`12` 四张示例卡均显示立绘、名称条、双属性和编号牌。
- [x] 交付格式：保存可检查精确尺寸的 SVG 源稿，并确定性导出 PNG 预览稿，不覆盖旧版本。
  - 证据：V4 SVG 与 1672×941 PNG 均已生成；V1～V3 文件未覆盖。
- [x] 方案文档：更新临时原型说明，以等宽矢量稿作为当前实现基准，旧生成图仅保留为对照。
  - 证据：`preparation-stage-ui-prototype-v4.md` 记录精确尺寸、坐标和版本结论。
- [x] 原型边界：不补充商店、费用、出售、刷新、锁定、升级或下一轮按钮规则。
  - 证据：V4 原型只包含战斗槽、发牌反馈、编号卡池和滚动条。
- [x] 框架边界审计：仅修改 `AutoDoc/Temp/` 原型产物，不修改 Unity、`Assets/`、代码或正式资源流程。
  - 证据：新增文件均位于 `AutoDoc/Temp/`；现有 `Assets/Art/ConceptArt` 图片仅作为只读立绘来源。
- [x] 结束审计：逐项记录实际尺寸与文件证据，只执行一次 `AutoDoc/CleanupTempDocs.bat`，清理后创建报告。
  - 证据：清理脚本退出码为 `0`，清理前后文件数均为 `80`；随后创建同任务报告。
