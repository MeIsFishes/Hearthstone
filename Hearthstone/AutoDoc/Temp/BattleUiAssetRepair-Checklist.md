# Battle UI 资产修复检查清单

- [x] **通过：确认报错根因与直接依赖。**
  - 证据：`BattleStages` 精确加载 `Resources/Ui/Battle`；修复前 `Assets/Resources/Ui/` 中无 Battle Asset、Prefab 或编辑场景，Console 只记录该缺失错误。
- [x] **通过：按 `bbxcommon-ui` 完成 Battle 页面与新增 UiScene 的完整落地。**
  - 证据：已生成 BattleView、BattleCardItem、Battle UI 编辑场景、导出 Asset 与预加载映射；既有 Stage 注册保持使用框架公开入口。
- [x] **通过：Battle View Prefab 完整。**
  - 证据：`Assets/Resources/Ui/BattleView.prefab` 根节点挂 `BattleView`；Editor 验收确认两个 `UiList`、TurnText、ResultText 引用完整，`BbxUiItems` 恰含两个列表。
- [x] **通过：BattleCardItem View Prefab 完整且可复用。**
  - 证据：`Assets/Resources/Ui/BattleCardItem.prefab` 根节点挂 `BattleCardItemView`；背景、高亮、阵亡遮罩、槽位、攻击和生命引用均非空。
- [x] **通过：Battle UI 编辑场景可重复导出。**
  - 证据：`Assets/Scenes/Ui/Battle.unity` 含 Canvas、1920×1080 CanvasScaler、GraphicRaycaster、UiSceneExporter、Main Group；BattleView 实例状态为 `PrefabInstanceStatus.Connected`。
- [x] **通过：Battle UiSceneAsset 由编辑场景实际导出。**
  - 证据：`Assets/Resources/Ui/Battle.asset` 只有一个导出项：PrefabPath=`Ui/BattleView`、Group=0、DefaultShow=true、Position=0、Scale=1、Pivot=(0.5,0.5)。
- [x] **通过：Resources 路径、Stage 注册和动态条目预加载一致。**
  - 证据：Editor 内 `Resources.Load<UiSceneAsset>("Ui/Battle")` 成功；`PreLoadUiData` 将 `Hearthstone.BattleCardItemController` 映射到 `Ui/BattleCardItem`。
- [x] **通过：Unity MCP 端到端链路可用。**
  - 证据：包与 Server 固定为 CoplayDev v10.0.0，stdio 配置有效；实例 `Hearthstone@e97c0c17` 被发现，`manage_scene(get_active)` 和 `read_console(error)` 均成功。
- [x] **通过：查清工具列表误判。**
  - 证据：Unity MCP 工具不是缺失，而是作为延迟工具位于 `functions.exec` 的 `ALL_TOOLS`；枚举到 42 个 `mcp__unityMCP__*` 工具并已实调用。最初只检查顶层显式工具清单的判断已纠正，无需重载会话。
- [x] **通过：更新 UI 落地的单篇底层 skill 文档。**
  - 证据：仅修改 `.codex/private-skills/bbxcommon-ui/SKILL.md`，明确新增 UiScene 必须在 Unity Editor 创建/保存编辑场景、放置保持连接的 Prefab、实际导出 Asset、按 Stage 精确 Resources 路径验证；动态条目还要执行 `Pre-UiInit` 和 `Export as Pre-load`。
- [x] **通过：符合 `add-skill` 的既有 skill 维护约束。**
  - 证据：保留 `bbxcommon-ui` 的英文小写 name、精简 description、中文正文和现有有效引用；未创建平行 skill，也未改变 agent 关联。
- [x] **通过：代码改动必要且最小。**
  - 证据：仅把 `PreLoadUiData.UiDatas` 初始化为 `new()`；否则首次执行正常的 `Export as Pre-load` 会访问空字典。没有新增函数、字段或一次性抽象。
- [x] **不适用：同步正式程序、美术或玩家视角文档。**
  - 证据：在 `AutoDoc/Program/`、`AutoDoc/Art/`、`AutoDoc/Design/` 未找到 Battle UI、UiScene 或 BattleView 的直接相关现状文档；用户指定的流程说明已写入既有 UI skill，未读取或修改 `AutoDoc/DesignPlan/`。
- [x] **通过：框架边界审计。**
  - 证据：静态 UI 全部保存在 Prefab；UI 编辑场景是导出配置源；Stage 使用 `GetOrCreateUiScene`/`SetUiScene`；动态条目使用 UiList 与框架预加载/池流程；未手写 `UiObjectDatas`、Scene/Prefab/Asset YAML 或运行时拼装整页 UI。
- [x] **通过：编译、结构、资源与 EditMode 验证。**
  - 证据：Unity 刷新编译未产生新增错误；结构化 Editor 验收全部通过；EditMode 8/8 通过；最终活动场景为未脏的 `Assets/Scenes/Main.unity`，Console error 为 0。按项目默认规则未进入 Play Mode，游戏内视觉与完整 Stage 卸载/重入未执行。
- [x] **通过：无已发现的无关改动。**
  - 证据：修改范围限于一处框架字段初始化、一个既有 UI skill、Battle UI 资产链路和本任务文档；`.meta` 仅由 Unity Editor 随新资产自动管理，未手工创建、编辑或删除。
- [x] **通过：结束审计完成。**
  - 证据：本清单已逐项依据实际文件与 Unity MCP 结果复核；下一固定收尾动作是仅运行一次 `AutoDoc/CleanupTempDocs.bat`，随后创建独立报告。
