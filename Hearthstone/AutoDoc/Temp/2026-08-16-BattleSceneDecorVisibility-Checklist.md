# 战斗场景装饰可见性调整检查清单

- [x] 匕首仍位于左上角并保持刀尖朝左下。证据：`localScale.x = -1` 保持不变，针对性测试通过。
- [x] 匕首露出更多刀身与护手，同时保留越界裁切、不过度侵入战斗区。证据：最终位置为 `(220, -280)`，尺寸保持 `420 × 630`，主体几乎完整、仅手柄顶端轻微越过上缘，卡牌层级仍更高。
- [x] 战场羊皮纸面板的做旧层明显增强，且继续位于卡牌与交互内容下方。证据：Alpha 由 `24%` 提高到 `42%`，节点创建顺序未变且不接收射线。
- [x] 通过 `BattleViewUiBuilder` 修改布局，并由 Unity 重建 `BattleView.prefab`。证据：Unity `execute_code` 返回 `BattleView prefab rebuilt with aged background.`，Prefab 序列化值为 `a: 0.42` 与 `(220, -280)`，背景 Sprite 测试确认为 `BattleBoardBackgroundAged`。
- [x] 更新并通过对应的 Editor 自动化测试。证据：两个针对性 EditMode 测试 `2/2` 通过。
- [x] 同步当前实现涉及的设计、美术与程序文档。证据：战斗设计、美术总览、UI 美术、战斗卡美术模块与战斗 UI 程序文档均已同步。
- [x] 未修改或回退工作区中的无关改动，未手工编辑 `.meta` 文件。证据：文本修改限于任务相关 Builder、测试、文档；Prefab 由 Unity Builder 生成。
- [x] 任务结束前完成产物与验证证据复核。证据：代码、Prefab 序列化参数、新底图尺寸与 Meta、旧值残留、原共享底图状态、文档与测试结果均已复核；文本源码/文档无 `git diff --check` 问题，Unity 生成 Prefab 保留空字段尾随空格。
- [x] 仅在终审后运行一次 `AutoDoc/CleanupTempDocs.bat`，并记录清理结果。证据：终审后执行一次，退出码为 `0`。
- [x] 根据追加反馈，将匕首调整为几乎完整露出，仅保留轻微越界裁切。证据：最终锚定位置 `(220, -280)`，镜像朝向与 `420 × 630` 尺寸保持不变。
- [x] 核验做旧层对战场主面板的实际影响；因现有透明纹理视觉不足，直接增强战斗主面板素材。证据：新建战斗专用 `BattleBoardBackgroundAged.png`，内侧直接绘入可见水渍、磨痕、短划痕和边缘积垢；原 `BattleBoardBackground.png` 保持 Git 干净，不影响备战推荐与词条窗。
- [x] 重新重建 Prefab、更新验证断言，并复核增强后的主面板素材与文档。证据：Unity 重建成功；新底图为 `1672 × 941`，Meta 由 Unity 自动生成；两个针对性 EditMode 测试 `2/2` 通过；相关文档已同步。
