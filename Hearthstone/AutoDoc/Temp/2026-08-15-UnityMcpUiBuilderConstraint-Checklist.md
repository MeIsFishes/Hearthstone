# Unity MCP 与 UiBuilder 项目约束调整检查清单

- [通过] 用户要求：项目不再强制要求使用 MCP 操作 Unity；保留 MCP 作为可选操作通道时，不得再把 MCP 不可用作为一般 Unity 资产任务的硬阻塞。
  - 证据：`AGENTS.md` 已将 MCP 改为可选通道，并规定未强制选用 MCP 时改用项目允许的其他 Editor 操作通道。
- [通过] 用户要求：UI Prefab 允许使用 `UiBuilder` 类创建。
  - 证据：`.codex/private-skills/bbxcommon-ui/SKILL.md` 新增 §2.7，并在普通页面、修改页面、Hud 与 UiScene 准备步骤中接入该方式。
- [通过] 用户要求：`UiBuilder` 固定放在 `Assets/Scripts/<项目名>/Ui/Editor/`。
  - 证据：UI skill §2.7.1 写明固定路径，并给出本项目 `Assets/Scripts/Hearthstone/Ui/Editor/` 示例。
- [通过] 用户要求：每个 Prefab 对应一个独立 `UiBuilder`。
  - 证据：UI skill §2.7.2 要求 Prefab 与最终构建入口一一对应，禁止多用途 Builder 维护多个 Prefab。
- [通过] 用户要求：`UiBuilder` 不注册到 Unity 项目菜单栏，使用时由 MCP 直接启动。
  - 证据：UI skill §2.7.4 禁止 `[MenuItem]` 和自动执行入口；§2.7.5 规定由 Unity MCP `execute_code` 直接调用公开静态 `Build()`。
- [通过] 修改根级项目约束和 `bbxcommon-ui` 流程，并检索、修正本次范围内与新规则冲突的强制表述。
  - 证据：已修改 `AGENTS.md` 与 UI skill，并对 `AGENTS.md`、`.codex/`、`Assets/Scripts/` 和正式现状文档执行 MCP/UiBuilder 关键字检索。
- [通过] 同步主代理入口、`recover-unity-mcp` 触发范围及其故障矩阵入口，确保一般 Unity 任务不再被 MCP 恢复流程强制阻塞。
  - 证据：`.codex/agents/MainAgent/MAIN_AGENT.md`、`recover-unity-mcp/SKILL.md` 和 `references/failure-matrix.md` 均已把恢复前提收敛为“本次明确选用 MCP”。
- [通过] Skill 编辑审计：保持既有底层 skill 路径、英文 `name`、简短中文 `description` 和中文正文；没有条件性内容需要拆分时不新增平行文件或关联。
  - 证据：`bbxcommon-ui` 与 `recover-unity-mcp` 仍位于既有 `.codex/private-skills/` 目录，frontmatter 校验通过；只沿用既有故障矩阵条件引用，没有新增 skill 或 agent 关联。
- [通过] 框架边界审计：UiBuilder 仅作为 Editor 配置源/构建入口，产物仍遵循 BbxCommon View、Controller、UiScene、导出与 Resources 流程，不允许运行时静态拼装或手写 Prefab/Scene/Asset YAML。
  - 证据：UI skill §2.7.3、§2.7.6、§2.7.7 明确保留 View/Controller、Resources、UiScene 与 Exporter 边界；根约束和 UI 场景流程继续禁止手写 YAML。
- [通过] 无关文件审计：不创建、修改或删除 `.meta`，不误改用户其他文件。
  - 证据：本次写入仅涉及 `AGENTS.md`、主代理规则、两个既有 skill 及其引用、任务清单；未执行任何 `.meta`、代码或 Unity 资产写操作。
- [不适用] 玩家视角设计文档同步。
  - 证据：已完整读取 `design-doc-format`；本次只改变代理/Editor 开发流程，没有玩家可见界面、入口、操作或功能现状变化，因此不修改 `AutoDoc/Design/`。
- [不适用] 美术文档同步。
  - 证据：已完整读取 `art-doc-writer` 并核对 `AutoDoc/Art/UI/ui-art-overview.md`；本次没有图片、Prefab 视觉内容或美术规格变化，因此不修改 `AutoDoc/Art/`。
- [不适用] 程序文档同步。
  - 证据：已完整读取 `program-doc-format` 并核对 `AutoDoc/Program/UI/battle/battle.md`；本次没有修改任何已实现 UI、接口或运行时流程，变化由根约束与底层 UI skill 承载，不应写入界面现状文档。
- [通过] 验证：检查 Markdown、路径与规则互相一致；确认 `UiBuilder` 规则覆盖新增/修改 UI Prefab 及必要的 Unity 资产流程。
  - 证据：静态断言已验证可选 MCP、恢复触发、Builder 路径、一对一关系、无菜单、`execute_code`、`Build()` 和禁止 YAML；UI skill 的全部相对引用均存在。
- [通过] 结束审计：逐项标记通过/未通过/不适用并写证据，只运行一次 `AutoDoc/CleanupTempDocs.bat`，随后创建同名 `*-Report.md`。
  - 证据：逐项复核已完成；`AutoDoc/CleanupTempDocs.bat` 已且仅已执行一次，退出码为 0；同名报告已在清理后创建。
