# 首轮随机发牌数据加载修复检查清单

- [x] 通过——复现并确认 `OnAwake()` 调用 `CreateDefault()` 时卡牌 CSV 尚未进入 `DataApi` 的根因。
  - 证据：用户堆栈定位到 `OnAwake()`；框架代码显示 `OnAwakeStage()` 只把 Game Engine Stage 加入加载队列，`InitReflectionAndResource` 与 `GameEngineDefault` 数据组在后续 `GameStage.LoadStage()` 中才实际加载。
- [x] 通过——将首轮随机奖励生成移动到卡牌配置已经可用的既有生命周期节点，不以空值兜底或硬编码奖励绕过问题。
  - 证据：`OnAwake()` 不再调用 `CreateDefault()`；首次 `OnStageLoadingCompleted()` 在基础数据 Stage 完成后创建默认随机奖励并请求战斗。
- [x] 通过——保持首次及后续每回合从 `1~98` 中随机发 5 张的既有规则、已持有排除、批次幂等和 StageGroup 数据契约。
  - 证据：`PreparationRewardBatchFactory`、`BattleStageStartupData` 构造、后续轮次入口和奖励应用逻辑均未改变；只调整首轮工厂调用时机。
- [x] 通过——补充或调整回归测试，覆盖无卡牌配置时默认启动数据不会提前读取 `DataApi`，以及配置可用后的首轮随机奖励生成。
  - 证据：新增 `EngineDefersDefaultRandomRewardUntilGameEngineDataStageHasLoaded`，断言 `OnAwake()` 不再创建默认奖励且加载完成回调负责创建；既有 `DefaultBattleRandomRewardExcludesInitialPlayerCards` 继续覆盖配置可用后的首轮随机批次。
- [x] 通过——检查误改无关文件、遗漏直接依赖以及用户现有工作区改动。
  - 证据：修复仅改游戏引擎启动时机、既有测试文件和直接相关程序文档；没有回退工作区其他 UI、Prefab、资源或文档改动。
- [x] 通过——检查新增函数与字段的复用价值，避免一次性抽象。
  - 证据：未新增函数或字段；直接使用框架已有 `OnStageLoadingCompleted()` 生命周期钩子与现有请求状态判定。
- [x] 通过——执行代码编译、相关 EditMode 测试或等价验证；默认不进入游戏。
  - 证据：`Hearthstone.csproj` 与 `Hearthstone.Tests.csproj` 均 0 错误编译；生命周期静态断言返回 True。Unity 编辑器当前未自动重跑新增测试，未进入游戏。
- [x] 不适用——玩家视角设计文档：读取基础格式 skill，核对玩家体验是否变化并按需同步。
  - 证据：已读取 `design-doc-format` 并核对现有备战设计文档；随机发 5 张的玩家体验未变化，本次只修正内部初始化时机，无需修改。
- [x] 不适用——美术文档：读取基础格式 skill，核对美术资产或视觉规范是否变化并记录结论。
  - 证据：已读取 `art-doc-writer`；没有修改图片、Prefab、布局或视觉规范。
- [x] 通过——程序文档：读取基础格式 skill，核对数据加载时机与 Stage 链路变化并按需同步。
  - 证据：已读取 `program-doc-format` 与玩法模块格式，并更新备战卡池程序文档，记录基础数据 Stage 完成后才生成首轮批次。
- [x] 通过——框架边界审计：确认使用既有 DataApi、GameStage 生命周期与 StartupData 契约，不残留早期加载绕行或平行状态。
  - 证据：修复使用框架明确提供的 `OnStageLoadingCompleted()`，不手动加载 CSV、不访问内部管理器、不修改数据格式或底层框架。
- [x] 通过——结束前逐项复核，只运行一次 `AutoDoc/CleanupTempDocs.bat`，随后创建同名报告。
  - 证据：本文件已完成逐项复核；清理脚本仅执行一次，退出码 0，随后已创建同名报告。
