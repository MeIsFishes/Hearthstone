# 核心战斗系统方案阶段报告

## 1. 任务结果

已完成核心战斗系统的第一阶段需求确认稿，明确攻击轮转、随机索敌、同时伤害、死亡跳过、胜负与平局规则，并保存到 `AutoDoc/Temp/Plan/2026-08-14-CoreBattleSystem-RequirementDraft.md`。

按项目 `game-module-design` 流程，数据、System、UI、GameStage 和完整 Todo 必须以用户确认后的功能点为输入，因此本阶段在需求确认点结束，未提前编写完整实施方案。

## 2. 检查项结果与证据

| 范围 | 状态 | 证据 |
|---|---|---|
| 用户核心规则覆盖 | 通过 | 确认稿 §1.1 覆盖双方 3 张、5/3、我方先攻、交替行动、左右轮转、随机索敌和同时伤害。 |
| 歧义收敛 | 通过 | 确认稿明确死亡即时生效、跳过死亡牌、双方空场判平局和随机种子复现。 |
| 当前项目调研 | 通过 | `Assets/Scripts/Hearthstone/` 仅存在 Placeholder 业务骨架；`Assets/Resources/` 与 `Assets/Scenes/` 仅有初始化和占位 UI 资产。 |
| 正式现状文档 | 不适用 | 项目不存在 `AutoDoc/Program/`、`AutoDoc/Art/`、`AutoDoc/Design/` 目录。 |
| 策划案隔离 | 通过 | 未搜索、枚举或读取 `AutoDoc/DesignPlan/`。 |
| 完整数据与架构方案 | 未通过 | 依流程等待用户确认功能点后设计，不是技术阻塞。 |
| UI/美术/UiScene 完整方案 | 未通过 | 依流程等待用户确认；已确认当前无正式卡牌和战斗美术可复用。 |
| GameStage 归属与实现顺序 | 未通过 | 依流程等待用户确认后补齐。 |
| 框架边界 | 通过 | 当前需求稿未提出绕过 ECS、Data、UI、Resource、GameStage 或 Editor 导出流程的实现。 |

## 3. 验证结果

- 文件存在性检查通过：需求确认稿已写入 `AutoDoc/Temp/Plan/`。
- 需求稿包含 5 条规则验收示例，可用于下一阶段自动测试设计。
- 未运行 Unity、编译或游戏验证；本阶段没有修改游戏内容，且用户只要求先出方案。
- 当前目录未检测到 Git 仓库元数据，因此没有可用的 `git status` 证据；文件范围通过直接路径检查确认。

## 4. 执行偏差

没有跳过规定流程。完整方案没有在本阶段强行展开，原因是 `game-module-design` 要求先提交功能点并获得用户确认。

## 5. 未解决风险

- “5/3”若代表 5 攻/3 血，则每次正常交战双方都会死亡；需要用户确认这正是原型期望。
- 死亡牌的跳过方式、最终同归于尽判平局以及最小占位 UI 范围仍需用户确认。
- GameStage 当前的数据加载管线在 DataGroup 后才进入首个 LateLoadItem；完整方案需要在初始化时序上保证战斗 System 在状态就绪前休眠，或收敛出最小框架缺口。

## 6. 文档处理与清理结果

- 未修改正式程序、美术、设计或策划案文档。
- 只运行一次 `AutoDoc/CleanupTempDocs.bat`，退出码 0。
- 清理后创建本报告；未删除任务产物。
