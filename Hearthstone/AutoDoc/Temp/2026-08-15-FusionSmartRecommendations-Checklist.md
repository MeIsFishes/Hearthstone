# 融合智能推荐任务检查清单

- [通过] 智能推荐按钮仅在融合槽至少存在一个单位时可交互。证据：`PreparationController.RefreshAll()` 使用 `evaluation.MaterialCount > 0`。
- [通过] 点击智能推荐按钮会打开融合页内模态弹窗，并提供明确关闭入口。证据：Prefab 中已序列化推荐 Overlay、Button 与 CloseButton。
- [通过] 推荐组合全部包含当前融合槽内的所有单位。证据：规则先固定复制全部槽内卡号，再只枚举补充候选。
- [通过] 推荐组合仅包含玩家当前持有、可作为融合材料且互不重复的单位。证据：候选遍历 `1~98`、检查 `HasCard()` 并排除已选卡。
- [通过] 推荐组合的卡牌编号总和必须正好为 99，且材料数量满足现有融合规则。证据：按 2～4 张枚举，并只在剩余和为 0 时提交评估。
- [通过] 推荐组合通过统一融合规则校验，包括配方存在、结果未持有等现有合法性条件。证据：每个候选最终复用 `EvaluateFusion().CanFuse`。
- [通过] 弹窗展示全部合法组合，不存在合法组合时显示“无”。证据：Controller 遍历完整结果列表，空列表写入“无”。
- [通过] 每条组合中已位于融合槽内的单位具有清晰高亮表现。证据：当前素材使用 TMP `<mark>` 暖金底色、浅金色和粗体。
- [通过] 推荐弹窗支持结果较多时滚动浏览，不在刷新循环中动态创建静态 UI。证据：Prefab 静态 ScrollRect + 单一 Rich Text Content，打开时仅更新文本与高度。
- [通过] 页面隐藏、关闭和重入时推荐弹窗状态与事件监听正确清理。证据：Hide、Close、Open、切页及两类 Revision 变化均关闭弹窗；按钮监听只在 `OnUiInit()` 注册一次。
- [通过] 为推荐枚举边界、包含关系、精确 99、去重及无结果场景补充自动化测试。证据：新增两项 NUnit 测试并由 `Hearthstone.Tests.csproj` 编译通过。
- [通过] View、Controller、UiBuilder 与 Prefab 序列化引用保持一致。证据：Builder 已生成 Prefab，五项新 View 引用均为非零 fileID。
- [通过] 核对 BbxCommon UI 框架边界与生命周期约束。证据：静态层级位于 Prefab，View 只持有引用，Controller 负责交互，规则状态仍位于 ECS/规则层。
- [通过] 同步程序、玩家视角设计与美术现状文档。证据：五份既有备战相关现状文档已更新。
- [通过] 执行可用的编译、测试或静态结构验证并记录结果。证据：两个工程均 0 编译错误；`dotnet test` 未发现可直接执行的 Unity 测试；Prefab 与规则完成静态检查。
- [未通过] 保留工作区内与本任务无关的既有和并行改动，不修改任何 `.meta` 文件。并行 PNG 内容均未被改动，但 Builder 的后台 Unity 导入在执行期间自动为这些图片补齐了未跟踪 `.meta`；依照禁止删除/编辑 `.meta` 的约束保留原状。
- [通过] 结束前逐项审计，运行一次临时文档清理并生成任务报告。
