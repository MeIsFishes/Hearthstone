# 图鉴与解锁存档检查清单

- [x] 核对现有卡号、普通/三卡/四卡融合分类与实际资源范围，图鉴总数由规则计算，不把示例 `136` 直接硬编码。通过：实际为 `98 + 49 = 147`。
- [x] 明确图鉴所属 MainMenuStage、MainMenu UiScene、UiGroup、默认显隐与主菜单往返关系。通过。
- [x] 新增唯一一对图鉴 View/Controller；静态层级由独立 UiBuilder 生成，动态卡牌通过既有 `BattleCardItem` Prefab、`UiList` 与对象池创建。通过。
- [x] 图鉴界面视觉接近备战卡牌库，展示除四卡融合卡外的全部可收集卡，并在左上角显示“已解锁/总数”。通过。
- [x] 从未解锁的卡显示明确锁定状态，不泄露其完整卡面信息；已解锁卡显示标准共享卡面和词条悬停说明。通过。
- [x] 点击已解锁卡可在灰色全屏蒙板上放大查看；点击卡面外空白处后，卡牌向屏幕底端移动并缩至 `0.3`，完成后关闭预览。通过。
- [x] 主菜单新增“图鉴”按钮，延续常态纯文字、悬停显示低饱和湿润羊皮纸底纹的样式。通过。
- [x] 主菜单右上角新增红色“清除数据”调试按钮，并确保只清理图鉴解锁存档、立即刷新显示且不会误删其他文件。通过。
- [x] 实现独立的卡牌永久解锁存档，保存到 Windows C 盘的程序本地应用数据目录；使用稳定、可校验、可恢复的文件格式与安全写入流程。通过。
- [x] 在玩家抽到或融合获得卡牌的权威成功路径记录永久解锁；首次创建存档时兼容当前局已经拥有的卡。通过。
- [x] 使用 UI 编辑场景作为 MainMenu UiScene 配置源，加入 connected 图鉴 Prefab、重新导出 UiSceneAsset，不手工编辑 Prefab/Scene/Asset YAML 或 `.meta`。通过。
- [x] 补充 EditMode 测试，覆盖卡池范围、总数、锁定判定、存档读写/清除、主菜单入口、图鉴 Prefab、UiScene 导出和预览收纳状态机。通过。
- [x] 完成 Unity 编译、相关 EditMode 测试与最终 Console 错误检查；默认不进入 Play Mode。通过：完整 EditMode `100/100`，最终 Console `0` error。
- [x] 玩家视角设计文档：读取适用格式，核对并更新开始界面与图鉴当前体验。通过。
- [x] 美术文档：读取适用格式，核对并更新主菜单、图鉴与通用 UI 资产现状。通过。
- [x] 程序文档：读取适用格式，核对并更新主菜单、图鉴、存档及受影响 UI/玩法入口。通过。
- [x] 框架边界审计：不在 Controller 运行时搭建整页静态层级，不重复实现对象池，不绕过 UiScene/UiApi/ResourceApi/GameStage 生命周期，不把永久权威状态只存于 View。通过。
- [x] 复核无关工作树改动、对象引用、事件清理、文件路径边界与异常恢复。通过：只操作本任务文件，保留既有工作树改动。
- [x] 结束前逐项审计，只运行一次 `AutoDoc/CleanupTempDocs.bat`，随后生成对应 Report。通过。
