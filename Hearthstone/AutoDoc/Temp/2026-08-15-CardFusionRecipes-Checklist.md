# 卡牌融合公式与备战栏改造检查清单

- [通过] 用户需求：融合结果生成独立新卡，融合卡编号从 100 开始并跳过 99。
  - 证据：`BattleCardCsvData.csv` 连续配置 100～148；`RunCardRules` 将 99 定义为锁位、100～148 定义为融合卡。
- [通过] 用户需求：每种不同的融合组合（组合而非排列）只对应一张不同新卡；输入顺序不影响结果。
  - 证据：CSV 配方按种类 ID 升序保存；读取时验证排序和重复，查询前在无分配路径中排序；数据脚本校验重复配方为 0、未排序配方为 0。
- [通过] 用户需求：卡牌 CSV 增加融合组合列，以基础卡牌种类 ID 的 `List<int>` 语义存储，并在读取 CSV 时预存到内存以供查询。
  - 证据：新增 `FusionRecipeTypeIds` 列与 `List<int>` 字段；`SetData` 阶段把规范化配方注册为 `DataApi` 负整数键。
- [通过] 用户需求：融合卡本身属于独立卡牌种类，可正常进入既有卡牌数据与玩法流程。
  - 证据：100～148 的 `CardTypeId` 与卡号一致，均在 `BattleCardTypeCsvData.csv` 中拥有独立类型和名称；融合实例使用结果卡号创建。
- [通过] 用户需求：保留并实现示例公式。
  - 证据：1;1→100 精锐战士、1;5→104 巨魔战士、1;4→103 野猪骑士、2;3→106 爆裂箭射手、4;5→113 贪婪巨魔、4;4;5→147 野猪驱使者、2;5→108 巨弓射手（巨魔）。
- [通过] 用户需求：结合现有基础卡牌、融合上限和槽位结构，补全其余有效融合组合与新卡配置，避免排列重复。
  - 证据：5 种基础卡的全部二重组合 15 条、全部三重组合减去 5;5;5 后 34 条，共 49 条。
- [通过] 用户补充：只配置 2 张与 3 张基础类型的组合结果，共 49 张；不配置任何 4 类型公式。
  - 证据：数据校验结果为 `PairRows=15`、`TripleRows=34`、`FusionRows=49`；运行时配方长度仅接受 2 或 3。
- [通过] 用户补充：选择 4 张材料时，以卡牌编号（现有点数）最高的 3 张决定融合公式和结果卡；4 张材料仍全部参与消耗、攻击/生命相加与词条合并。
  - 证据：`EvaluateFusion` 无分配维护卡号最高三张；`TryFuse` 仍遍历四个融合槽汇总属性、合并词条并消费所有材料；编辑器测试覆盖该分支。
- [通过] 用户补充：融合结果类型 CSV 的攻击、生命和初始词条单元格留空，运行时属性完全以融合结算为准。
  - 证据：99～148 的五个相关单元格均为空；数据校验 `RuntimeStatRowsNonBlank=0`；加载器验证运行时组合类型不得配置静态属性。
- [通过] 用户需求：备战栏可见 100 号及之后的融合卡卡位；99 号卡位使用特殊带锁卡图锁住。
  - 证据：卡池范围扩至 148、22 行并按运行时高度展开；99 分支显示 `FusionCard_099` 锁图且禁用点击拖拽，100～148 为正常固定卡位。
- [通过] 用户需求：确认并落实巨魔最多只能参与 2 个融合时，不为 3 个、4 个巨魔组合预留槽位。
  - 证据：无 5;5;5 配方，数据校验 `TripleOgre=0`；运行时读取验证食人魔数量不得超过 2；本次不存在四卡公式卡位。
- [通过] 数据规范：沿用现有 `CsvDataBase` / `DataApi` 加载路径；新增列解析、内存索引、CSV 表头说明与 Associated 注释均符合 `config-data-design`。
  - 证据：两表继续继承既有 CSV 基类并使用既有 DataGroup；列说明和 Associated 行已同步。
- [通过] 数据规范：确认表资源 key、DataGroup、加载时机和按 ID 登记方式有效；融合组合键查询为 O(1) 或等价的预构建查询。
  - 证据：保留既有资源 key 与加载阶段；结果仍按正卡号登记，配方额外按无碰撞负键登记，查询为 `DataApi.GetData`。
- [通过] 代码质量：新增字段和函数具备实际复用价值，不保留无意义的一次性抽象；不产生不必要的重复分配或 GC。
  - 证据：组合排序使用三个局部整数，不在每次评估时创建集合或排序列表；配方 List 只在 CSV 读取时创建。
- [通过] 兼容性：既有基础卡、购买/生成、融合、部署、展示等直接依赖在新编号和新增卡牌下保持兼容。
  - 证据：基础卡 1～98 数据未改变；存储扩至 149 个索引；融合卡沿用既有 `RunCardInstanceData`、拥有状态、卡池与战斗槽流程；项目测试程序集编译通过。
- [通过] UI/资源：确认 99 锁位所需现有或新增视觉资源、卡位顺序和展示行为均已接通。
  - 证据：`FusionCard_099.png` 已替换为带锁铁门图；其既有资源 key 与 `.meta` 保持不变；控制器锁位路径和卡池高度已接通。
- [通过] 验证：运行适合本项目的静态检查、CSV 数据校验与自动化测试/编译检查；按项目说明默认不进入游戏验收。
  - 证据：`dotnet build Hearthstone.Tests.csproj --no-restore` 为 0 错误、8 个既有程序集版本警告；CSV 校验 49/15/34、无重复、无缺类型；Unity Editor 最新日志尾部无编译错误。按项目默认未进入 Play Mode，且因编辑器已打开未另启批处理 Test Runner。
- [通过] 框架边界审计：实现必须使用项目既有配置、资源、UI、对象生命周期和导出流程，不得引入平行加载、手写导出产物或绕过框架内部管理器。
  - 证据：仅扩展 `CsvDataBase`、`DataApi`、既有卡牌控制器和 `UiList` 使用路径；未增加平行加载器或框架管理器。
- [通过] 变更范围审计：检查工作区已有修改并避免误改、覆盖或回退用户及其他任务的无关变更；不创建、编辑或删除 `.meta` 文件。
  - 证据：基于脏工作区做增量修改并保留同时出现的融合揭示 UI/攻击表现等内容；`git diff --numstat -- '*.meta'` 无输出。
- [通过] 玩家视角设计文档：完整读取基础格式 skill，核对本次玩家可见融合规则、卡牌与备战栏现状是否需要新增或同步正式文档，并记录路径和证据。
  - 证据：已同步 `AutoDoc/Design/Specific/preparation-card-pool/preparation-card-pool.md` 与 `AutoDoc/Design/Specific/combat-system/combat-system.md`。
- [通过] 美术文档：完整读取基础格式 skill，核对新增融合卡视觉与 99 锁位素材现状是否需要新增或同步正式文档，并记录路径和证据。
  - 证据：已同步战斗卡、备战卡池模块文档与 UI 美术总览，记录 99 锁位视觉及资源。
- [通过] 程序文档：完整读取基础格式 skill，核对融合数据模型、内存索引和 UI 行为是否需要新增或同步正式文档，并记录路径和证据。
  - 证据：已同步备战卡池、备战 UI 和战斗系统程序文档，记录配方键、49 条组合、四卡选择与结算规则。
- [通过] 结束审计：重新打开本清单，逐项标记通过/未通过/不适用并补充证据；可修正缺口先修正后复核。
  - 证据：2026-08-15 结束审计已完成，未发现尚可修正的需求缺口。
- [通过] 结束清理与报告：只运行一次 `AutoDoc/CleanupTempDocs.bat`，随后创建同名 `*-Report.md`，记录结果、证据、验证、偏差、风险、文档处理与清理结果。
  - 证据：清理脚本只执行一次且退出码为 0；随后创建 `2026-08-15-CardFusionRecipes-Report.md`。
