# UI 按钮与备战区拖放音效任务检查清单

- [x] **通过**——用户要求：为所有 UI 按钮统一添加点击音，覆盖现有与后续通过项目 UI 框架创建的按钮。
  - 证据：`UiViewBase.InitBbxUiItem()` 在每个 View 首次初始化时扫描直属 `Button`（含 inactive 子层）并注册 `click_001`；当前主菜单、图鉴、备战、推荐条目和战斗结算按钮全部通过该生命周期创建。
- [x] **通过**——用户要求：卡牌拖放并成功进入备战区时播放一次短促“咔”声，不在取消拖放或无效落点时误播。
  - 证据：`PreparationController.DropCardOnSlot()` 仅在 `RunCardRules.TryPlaceCard()` 返回 `true` 时播放 `drop_001`，失败分支只刷新；放回原槽由规则返回 `false`。
- [x] **通过**——现状核验：定位项目 UI 按钮、卡牌拖放/备战区落位、音频播放和资源加载的既有实现与公开入口。
  - 证据：核验了 `UiViewBase` / `UiControllerBase`、所有 Hearthstone View 按钮字段、`BattleCardItemController.OnPreparationInteract()`、`PreparationController.DropCardOnSlot()`、`RunCardRules.TryPlaceCard()`、`AudioApi`、`AudioManager` 与 `ResourceApi.LoadAudio()`。
- [x] **通过**——框架边界审计：复用项目既有 UI、音频、资源与生命周期框架，不在业务层建立平行全局监听、音频管理或手写资源加载流程。
  - 证据：按钮音接入 BbxCommon View 初始化生命周期，落位音接入既有玩法成功入口；两处均调用公开 `AudioApi`，音频 key 由既有 `ResourceApi` 索引加载。框架改动局部、向后兼容，不改变公开契约、生命周期或数据格式。
- [x] **通过**——实现审计：完成所需代码/配置/资源修改，不误改无关文件，不遗漏直接依赖；不创建、编辑或删除 `.meta` 文件。
  - 证据：代码只增量修改 UI 框架初始化、备战落位入口和测试；复用已有两段音频，不改 Prefab/配置/音频资源。未手工创建、编辑或删除 `.meta`；Unity Editor 自动为新增测试脚本生成了其必要 `.meta`。
- [x] **通过**——代码质量：检查新增字段与函数是否确有复用价值，避免不必要的一次性抽象、重复分配与 GC。
  - 证据：按钮扫描每个 View 实例仅执行一次，池化重开不重复扫描/注册；静态回调无闭包分配；未新增音频管理器或业务对象池。
- [x] **通过**——行为验证：验证按钮点击音只在有效点击时播放，且不会因重复注册而一次点击多次播放。
  - 证据：监听挂在 Unity `Button.onClick`，禁用 Button 不触发；`m_ButtonClickAudioInitialized` 防止重复注册，最近父 `UiViewBase` 判定排除嵌套 View；两项 `UiButtonAudioTests` 均通过。
- [x] **通过**——行为验证：验证卡牌仅在成功拖放至备战区时播放“咔”声，并检查相关拖放分支。
  - 证据：`SuccessfulBattleSlotDropUsesTheDedicatedPlacementAudio` 通过；代码分支证明只有 `TryPlaceCard == true` 播放，取消、无效落点、放回原槽不进入播放分支。
- [x] **通过（有已知非本任务失败）**——回归验证：运行可用的静态检查、编译或项目测试；默认不进入游戏，若无法进行运行态听感验收则记录限制。
  - 证据：`BbxCommon.csproj`、`Hearthstone.csproj`、`Hearthstone.Tests.csproj` 分别构建成功，0 error；目标文件 `git diff --check` 通过。Unity EditMode 111 项中 107 通过、4 项既有玩法测试失败，本次新增 3 项全部通过。按项目默认未进入游戏，未做扬声器听感验收。
- [x] **通过**——玩家视角设计文档：完整读取基础格式 skill，核对现有相关文档与玩家可见变化，按实际现状决定更新、新增或不适用并记录证据。
  - 证据：完整读取 `design-doc-format` 及开始界面、战斗、局外养成专项格式；更新 `start-screen`、`preparation-card-pool`、`combat-system`、`meta-progression`，只记录已实现的按钮与落位声音反馈。
- [x] **不适用**——美术文档：完整读取基础格式 skill，核对音频资源是否落入美术/资产文档范围，按实际现状决定更新、新增或不适用并记录证据。
  - 证据：完整读取 `art-doc-writer`。本次复用已有 OGG，仅改变声音触发逻辑，不新增或修改 2D 图片、UI 视觉、图片引用或生成规格，因此 `AutoDoc/Art/` 不受影响。
- [x] **通过**——程序文档：完整读取基础格式 skill，核对现有相关文档与实现变化，按实际现状决定更新、新增或不适用并记录证据。
  - 证据：完整读取 `program-doc-format` 与 UI 界面格式；更新主菜单、备战、战斗和图鉴四篇 UI 程序文档，记录 `UiViewBase` 去重注册和成功落位音分支。
- [x] **通过**——收尾审计：重新读取本清单，逐项标记通过、未通过或不适用并附证据。
  - 证据：2026-08-16 收尾时已重新读取并逐项核对代码、资源唯一性、测试结果、文档和目标文件状态。
- [x] **通过**——临时文档清理：收尾时只运行一次 `AutoDoc/CleanupTempDocs.bat`，记录退出结果，随后创建对应任务报告。
  - 证据：已在项目根目录执行一次，退出码 `0`、无错误输出；随后创建同任务名报告，未重复运行。
