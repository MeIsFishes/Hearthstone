# 备战界面比例二次调整报告

## 结果

已完成备战页上下区域比例的二次调整。出战/融合页签移动到木质幕布左上角，出战卡整体上移，下方卡池底框适当收低；当前离线画面中，出战卡底部与卡池框顶边约保留 70 个参考像素的留白。

## 实施内容

- `BattleTab` / `FusionTab`：锚点位置调整为 `(215, -58)` / `(525, -58)`。
- `BattleSlotList`：锚点位置调整为 `(0, -130)`。
- `CardPoolPanel`：由 `1780 × 660 @ (0, 335)` 调整为 `1780 × 630 @ (0, 320)`；仍高于最初的 `1780 × 600`。
- 卡池内部 `ScrollRect`、`Viewport`、滚动条和轨道高度由 `570` 调整为 `540`，上下箭头同步调整到 `±258`。
- 更新 `RunCardRulesTests` 中的布局结构断言。
- 通过 Unity Editor 调用 `PreparationViewUiBuilder.Build()` 正式重建 `PreparationView.prefab`。
- 同步玩家视角设计、美术模块和程序 UI 现状文档；本次没有新增或重绘美术资源。

## 检查项与证据

| 检查项 | 状态 | 证据 |
| --- | --- | --- |
| 上下比例 | 通过 | 卡池面板为 `1780 × 630`，较上一版降低 30，仍较原版增高 30 |
| 页签位置 | 通过 | Builder、Prefab 与测试断言均为 `(215, -58)` / `(525, -58)` |
| 出战卡位置 | 通过 | Builder 与 Prefab 的 `BattleSlotList` 均为 `(0, -130)` |
| 上一轮功能不回退 | 通过 | 相关 EditMode 测试覆盖奖励提示、卡池标题、Continue Button 与字体结构 |
| 单一配置源 | 通过 | 仅调整 `PreparationViewUiBuilder` 布局常量，Prefab 由 Unity 重建 |
| 框架边界 | 通过 | 继续使用既有 View/Controller、UiBuilder、`UiList` 与资源路径，无平行实现 |
| 文档 | 通过 | Design、Art、Program 三类现状文档均已同步 |

## 验证

- Unity 编译：通过，控制台无编译异常。
- Unity EditMode：`Hearthstone.Tests.RunCardRulesTests` 19/19 通过。
- `dotnet build Hearthstone.Ui.Editor.csproj --no-restore`：0 错误；仅有项目既有的程序集版本冲突警告。
- `dotnet build Hearthstone.Tests.csproj --no-restore`：0 错误；仅有项目既有的程序集版本冲突警告。
- Prefab 结构检查：通过，序列化 Rect 参数与 Builder 一致。
- 1920×1080 离线视觉渲染：通过；页签、出战卡和卡池边界互不遮挡，上下留白符合本次调整目标。
- 临时预览对象检查：`__PreparationPreviewCamera` 与 `__PreparationPreviewCanvas` 均不存在。
- Play Mode：按项目约定未进入。

## 偏差与风险

- 本次仅调整布局比例，没有重绘或替换页签资源。
- 未进行实际 Play Mode 游戏验证；验证范围为 Unity 编译、EditMode、Prefab 结构与离线视觉渲染。
- 构建仍报告项目既有的 .NET 程序集版本冲突警告，但没有错误，不由本次改动引入。

## 清理

任务结束前已逐项复核检查清单，并且只运行一次 `AutoDoc/CleanupTempDocs.bat`；命令退出码为 `0`。本报告在清理完成后创建。
