# 主代理验收职责与 Code Reviewer 收敛检查清单

- [x] 通过：确认修改对象。更新既有底层策划案实现/写作规则、两个既有审查 agent 与主代理注册信息，未新增 agent 或平行关联。
  - 证据：修改范围为 6 个直接相关规则/agent 文件。
- [x] 通过：将 `design_plan_code_reviewer` 收敛为只读代码审查职责。
  - 证据：删除最终 Review 文档模式、验收材料输入、验收步骤和正式 Review 写权限；唯一输出限定为单个 Temp code review 文件。
- [x] 通过：同步 code reviewer 的完整 agent 契约。
  - 证据：description 改为“审查策划案实现的代码质量与框架边界。”；角色、输入、读写范围、禁止事项、8 个审查步骤和输出要求均只针对代码审查。
- [x] 通过：同步主代理注册表描述。
  - 证据：`MAIN_AGENT.md` 与 TOML description 语义一致，不再声明生成最终 Review。
- [x] 通过：主代理独立执行验收并编写逐篇验收 Review。
  - 证据：实现流程明确禁止把验收操作、通过判定或正式 Review 写作委托给执行、Plan review 或 code review 子代理。
- [x] 通过：补全流程log验收实际步骤。
  - 证据：规定 StageGroup 启动、启动时间/日志位置隔离、顺序操作、Unity Console/Editor Log 采集、Temp 证据路径和逐项映射判定。
- [x] 通过：补全游戏内截图验收实际步骤。
  - 证据：规定正式入口、目标状态、实际运行截图、DesignPlan media 归档、英文文件名及截图到 case/编号映射。
- [x] 通过：补全仅功能验收实际步骤。
  - 证据：规定生产功能入口或最小一次性脚本、明确输入/输出、Temp 结果路径、禁止复制生产逻辑和临时文件清理。
- [x] 通过：未要求游戏内截图时通过检查相关资产编排完成美术验收。
  - 证据：主流程要求记录资产路径、内容、引用/组合、层级/布局/状态编排、规格/状态覆盖和 `ART-` 映射；策划案与 Plan 审查规则已同步。
- [x] 通过：正式 Review 规则迁移到主代理流程。
  - 证据：实现流程定义通过门槛、失败回退路径、四个二级章节及验收三个小节、媒体规则、风险省略规则和复读检查。
- [x] 通过：保留既有门槛。
  - 证据：Plan 审查、最多两轮代码审查、`ART-`/`FUNC-` 分类、合并验收趟次、状态流转、提交/推送和最终 Git 清洁门槛均仍存在。
- [x] 通过：保持底层 skill 元数据有效。
  - 证据：`design-plan-doc` 与 `project-state-preflight` 的 name/目录一致，description 长度分别为 21、30，均不超过 40。
- [x] 通过：不拆分核心流程。
  - 证据：验收操作与 Review 写作属于实现 preflight 每次都必须掌握的连续门槛；未建立额外 skill 或参考文件。
- [x] 通过：验证 SKILL、Markdown、TOML、agent 元数据、注册与引用。
  - 证据：两份 agent TOML 经 Python `tomllib` 解析通过；两份 SKILL 等价 frontmatter 校验通过；code reviewer name 合法、description 长度 18、skill 引用均存在，注册表已同步。官方 `quick_validate.py` 因环境缺少 `PyYAML` 未能启动，非内容失败。
- [x] 通过：消除 code reviewer 编写最终/验收 Review 的正向残留指令，并检查文件范围。
  - 证据：无“最终 Review 文档模式”等残留；对 code reviewer 的剩余正式 Review 表述均为禁止/职责隔离规则。目标目录 `.meta` 检索数量为 0；当前目录不是 Git worktree，改用明确路径和全量文本检索审计。
- [x] 通过：框架边界审计。
  - 证据：游戏内验收强制完整读取 `test-game-content` 并通过 `GameStageEntryLauncher`/StageGroup 启动；Review 继续写入既有 DesignPlan Review/media 目录。
- [x] 不适用：正式程序、美术和玩家视角设计文档同步。
  - 证据：本次只修改工作流与 agent 职责，不改变游戏当前实现或玩家可见内容。
- [x] 通过：任务结束前重新读取清单并逐项记录证据。
  - 证据：已复核修改文件、职责检索、验收步骤、frontmatter、TOML、引用路径、注册表、`.meta` 和 Git 环境结果。
- [x] 通过：只运行一次 `AutoDoc/CleanupTempDocs.bat`，记录结果，并在清理后创建同名 `*-Report.md`。
  - 证据：清理前后均为 11 份临时 Markdown，脚本退出码 0；数量未超过 500，未删除文件。
