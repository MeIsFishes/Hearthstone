# UiBuilder MCP 完整工具发现规则检查清单

- [通过] 用户要求：在 `bbxcommon-ui` skill 中明确先通过当前环境的完整工具列表发现 Unity MCP，不能只查看顶层展示工具。
  - 证据：UI skill §2.7.5 明确要求检查“完整工具列表（完整工具目录）”，并禁止根据顶层提示或初始工具列表判断 MCP 不可用。
- [通过] 用户要求：发现可调用的 `unityMCP` 后，尽量优先通过 MCP `execute_code` 执行 UiBuilder。
  - 证据：UI skill §2.7.6 明确规定发现可调用 Unity MCP 后应优先使用项目固定 MCP 的 `execute_code` 调用 Builder `Build()`。
- [通过] 明确完整工具目录可能来自 `tool_search`、延迟工具目录或 `functions.exec` 的 `ALL_TOOLS`，并按 `mcp__unityMCP__*` 识别工具。
  - 证据：UI skill §2.7.5 已逐项列出三种正式发现入口和 `mcp__unityMCP__` 名称前缀。
- [通过] 明确读取实际 `execute_code` schema 后调用；完整目录未发现或调用失败时才进入恢复/替代分支。
  - 证据：UI skill §2.7.5 要求加载延迟工具实际 schema，§2.7.7 将恢复/替代流程限制在完整列表未发现或按实际 schema 调用失败之后。
- [通过] 保留 MCP 可选的项目总约束，不把一般 Unity 任务重新改成强制依赖 MCP。
  - 证据：修改仅位于选择 UiBuilder 后的 §2.7；§2.7.7 仍允许非强制 Builder 任务回到普通 Unity Editor Prefab 编辑流程，`AGENTS.md` 未修改。
- [通过] Skill 编辑审计：沿用既有底层 skill、frontmatter、中文正文和既有 agent 关联，不新增平行 skill 或无关引用。
  - 证据：仅修改既有 `.codex/private-skills/bbxcommon-ui/SKILL.md` 正文；frontmatter、路径和主代理索引保持不变。
- [通过] 框架边界审计：工具发现与调用方式不改变 UiBuilder、View/Controller、Resources、UiScene 和导出流程边界。
  - 证据：§2.7.8—§2.7.9 继续限制 Builder 职责并要求 UiScene/Exporter 后续流程，本次未改这些边界。
- [通过] 无关文件审计：不修改代码、Unity 资产或 `.meta` 文件。
  - 证据：除任务清单外仅编辑 UI skill Markdown；未执行代码、资源或 `.meta` 写操作。
- [不适用] 玩家视角设计文档同步。
  - 证据：已完整读取 `design-doc-format`；本次仅细化代理工具发现与 Editor 调用流程，没有玩家可见变化。
- [不适用] 美术文档同步。
  - 证据：已完整读取 `art-doc-writer`；本次没有美术资产、Prefab 视觉内容或规格变化。
- [不适用] 程序文档同步。
  - 证据：已完整读取 `program-doc-format` 并列出现有 UI 程序文档；本次没有运行时 UI、接口或 GameStage 变化，正式现状文档不应记录代理工具发现流程。
- [通过] 静态验证：检查完整工具列表、`unityMCP`、`execute_code`、优先调用和失败分支表述完整且互不冲突。
  - 证据：静态检索确认 `完整工具列表`、`tool_search`、`延迟工具目录`、`ALL_TOOLS`、`mcp__unityMCP__`、`execute_code`、实际 schema、优先调用和恢复分支全部存在。
- [通过] 结束审计：逐项写明状态与证据，只运行一次 `AutoDoc/CleanupTempDocs.bat`，随后创建同名报告。
  - 证据：逐项审计已完成；清理脚本已且仅已运行一次，退出码为 0；同名报告已在清理后创建。
