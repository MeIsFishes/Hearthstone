# 卡牌入场基准数值颜色检查清单

- [x] 明确攻击与血量“入场时数值”的权威采样时点，并为每个战斗卡牌 Entity 保存稳定基准值。
  - 证据：所有初始化入口最终由 `InitializeValues()` 将当时攻击与实际当前生命写入 `EntryAttack`、`EntryHealth`。
- [x] 当前攻击或血量低于各自入场基准时，卡面对应数字显示红色。
  - 证据：`GetStatTextColor()` 的小于分支返回 `#FF5C5C`，攻击和生命刷新均调用该映射。
- [x] 当前攻击或血量高于各自入场基准时，卡面对应数字显示蓝色。
  - 证据：`GetStatTextColor()` 的大于分支返回 `#58B0FF`，定向测试覆盖该分支。
- [x] 当前值等于入场基准时恢复既有默认文字颜色，攻击与血量分别独立判断。
  - 证据：相等分支返回 `Color.white`；两个刷新函数分别读取 `EntryAttack` 与 `EntryHealth`。
- [x] 备战卡池、出战槽与融合槽等非战斗展示不误用战斗基准颜色逻辑，保持原有颜色表现。
  - 证据：非战斗绑定时 `m_Card` 为空，刷新直接使用白色；隐藏与换绑也重置为白色。
- [x] 基准值随战斗实体创建初始化，实体回池时完整重置，不因后续增减益漂移。
  - 证据：测试先修改当前攻血再确认基准不变，并验证 `CollectToPool()` 后两项均归零。
- [x] 遵循 BbxCommon ECS：运行时权威状态放 RawComponent，不在 View 或 Controller 保存玩法状态。
  - 证据：基准字段位于 `BattleCardRawComponent`，Controller 只读取并决定文字表现。
- [x] 遵循 BbxCommon UI：View 只持有引用，颜色刷新继续由共享卡牌 Controller 执行，不新增平行 UI。
  - 证据：仅修改现有 `BattleCardItemController`，未修改 View、Prefab 或 Builder，备战与战斗继续共享卡面。
- [x] 检查数值变更后的现有刷新/监听链路，确保攻击和血量变化时颜色同步刷新。
  - 证据：现有 `AttackValue`、`CurrentHealth` 的 ModelWrapper Dirty Listener 分别调用 `RefreshAttack()`、`RefreshHealth()`。
- [x] 补充定向 Editor 测试，覆盖低于、等于、高于基准及组件初始化/回收。
  - 证据：`BattleCardStatTextColorComparesAgainstEntryValue` 与扩充后的运行时状态测试通过；相关测试 38/38。
- [x] 完成相关程序集编译与静态检查；默认不主动进入 Play Mode。
  - 证据：三个程序集均为 0 error；两个修改脚本 Unity 标准校验为 0 warning/0 error；定向 `git diff --check` 通过；未进入 Play Mode。
- [x] 检查工作区已有修改，不回退无关内容，不创建、编辑或删除 `.meta`。
  - 证据：修改限定在三份代码/测试、直接相关正式文档和本任务临时文档；`git diff --name-only -- '*.meta'` 无输出。
- [x] 检查新增字段与辅助函数是否必要，避免一次性抽象和重复颜色判断。
  - 证据：两个字段分别承载独立基准；单一 `GetStatTextColor()` 同时复用于攻击和生命，无重复分支。
- [x] 玩家视角设计文档：读取适用格式 skill，按实际玩家可见结果同步或说明不适用。
  - 证据：已完整读取 `design-doc-format` 与战斗系统格式，并更新 `AutoDoc/Design/Specific/combat-system/combat-system.md`。
- [x] 美术文档：读取适用格式 skill，按实际颜色规范同步或说明不适用。
  - 证据：已完整读取 `art-doc-writer` 及模块、UI、风格三份格式引用，同步三份现状文档；未新增图片资产。
- [x] 程序文档：读取适用格式 skill，按实际 ECS 数据链与 UI 刷新逻辑同步。
  - 证据：已完整读取 `program-doc-format`、战斗专项与 UI 界面格式，同步战斗系统和战斗界面程序文档。
- [x] 框架边界审计：确认未绕过 ECS 生命周期、共享卡牌 UI、既有监听刷新和 Prefab/Builder 配置源。
  - 证据：数据仍走 RawComponent 初始化/回收，表现仍走共享 Controller 与 ModelWrapper 监听；没有运行时拼 UI、平行页面或手写 Prefab。
- [x] 结束前逐项复核，仅运行一次 `AutoDoc/CleanupTempDocs.bat`，随后生成同名任务报告。
  - 证据：本清单已逐项复核；下一步仅执行一次清理并在清理后创建报告。
