# 备战卡池滚动修正检查清单

- [x] 通过——删除备战卡池滚动期间逐帧触发的诊断日志，不影响其他必要错误或业务日志。
  - 证据：移除 `ScrollRect.onValueChanged` 注册、`OnCardPoolScrollChanged`、页面状态打印以及备战 Controller 其余普通 `DebugApi.Log`；`DebugApi.LogError` 仍保留运行状态错误报告。
- [x] 通过——将备战卡池滚动速度在当前基础上提高 50%。
  - 证据：`PreparationController.OnUiInit()` 对 Prefab 基础 `scrollSensitivity=45` 执行 `*= 1.5f`，运行值为 `67.5`。
- [x] 通过——鼠标悬停在卡片上时，滚轮事件仍能驱动备战卡池滚动。
  - 证据：`BattleCardItemController` 实现 `IScrollHandler`，仅在 `CardPool` 绑定模式把事件交给 `PreparationController.ForwardCardPoolScroll()`，后者调用现有 `ScrollRect.OnScroll()`。
- [x] 通过——保持滚动实现复用 Unity `ScrollRect`、现有 `UiList` 与 View/Controller 生命周期，不引入平行滚动实现。
  - 证据：未新增滚动组件、列表或每帧逻辑；转发入口直接复用页面序列化的 `CardPoolScrollRect`。
- [x] 通过——检查修改范围、直接依赖与现有用户改动，避免误改无关文件。
  - 证据：修改限于两个相关 Controller、既有 Editor 测试和三篇直接相关文档；未覆盖工作区中的既有 Prefab、美术资源及其他修改。
- [x] 通过——检查新增函数和字段是否确有复用价值，避免不必要的一次性抽象。
  - 证据：仅新增页面级滚轮转发入口；未新增字段、组件类型或平行事件系统，并删除日志专用回调和已无用途的参数。
- [x] 通过——验证相关 C# 代码与 Prefab/Builder 配置一致，并执行可用的静态或编译验证。
  - 证据：Prefab 与 Builder 的基础灵敏度均保持 `45`，运行时统一乘 `1.5`；`Hearthstone.csproj` 和 `Hearthstone.Tests.csproj` 编译均为 0 错误，`git diff --check` 通过。
- [x] 通过——框架边界审计：确认未绕过 BbxCommon UI、公开 API、生命周期、编辑器配置源或导出流程。
  - 证据：继续使用 BbxCommon View/Controller、`UiList` 和 Unity `ScrollRect`；没有直接访问 UI Manager、手写 Prefab/Asset 或改变 UiScene 导出信息。
- [x] 通过——玩家视角设计文档已同步。
  - 证据：已读取 `design-doc-format` 并更新 `AutoDoc/Design/Specific/preparation-card-pool/preparation-card-pool.md`，记录 50% 加速和卡片命中区域内可滚动的当前体验。
- [x] 不适用——美术文档无需修改。
  - 证据：已读取 `art-doc-writer` 和现有备战卡池美术模块文档；本次未改变 Sprite、色彩、布局、Prefab 视觉或资源引用。
- [x] 通过——程序文档已同步。
  - 证据：已读取 `program-doc-format`、UI 界面格式及相关文档；更新备战 UI 文档的滚轮转发、灵敏度和日志现状，并校正卡池缩放；同步将玩法文档卡面比例校正为 `25:36`。
- [x] 通过——结束前逐项复核，运行一次 `AutoDoc/CleanupTempDocs.bat`，并生成对应任务报告。
  - 证据：清理脚本返回退出码 `0`；任务报告已在清理后创建。
