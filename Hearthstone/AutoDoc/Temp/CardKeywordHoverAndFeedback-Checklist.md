# 卡牌词条悬浮说明与反馈强化检查清单

- [x] 号角与弓箭反馈图标尺寸明显增大，且不超过卡面可读范围、不互相遮挡。——两者均调整为 96×96，并分别布置于卡牌上中部左右两侧。
- [x] 号角与弓箭反馈图标的上飘距离明显增加，缩放、位移与渐隐仍受统一动画播放系数影响。——上飘距离由 48 调整为 84，反馈计时继续乘用 `AttackPresentationPlaybackSpeed`（当前 0.8）。
- [x] 不适用：冲锋发动时只播放一次号角音效，音效经项目公开音频接口与现有资源规范接入。——用户后续明确要求移除号角音效，相关代码、测试引用及 WAV/MIDI/乐谱资源均已删除。
- [x] 不适用：号角音效触发时机与号角图标反馈一致，并兼容统一动画播放系数涉及的延迟。——音效需求已撤回；全项目检索无 `ChargeHornCall`、`BattleChargeHorn` 或 `PlayChargeHorn` 残留。
- [x] 卡牌 Prefab 新增静态悬浮说明窗口，采用棕色边框与木板底板，静态结构由 BattleCardItemUiBuilder 维护。——Builder 复用 `BattleBoardBackground.png`，创建棕色描边、木板底图的静态 Tooltip 层级。
- [x] BattleCardItemView 仅保存悬浮说明窗口的序列化引用；显隐、文本和交互由 BattleCardItemController 管理。——View 新增根节点与 TMP 文本引用，行为均集中在 Controller。
- [x] 鼠标悬停在含词条卡牌上时显示词条说明，移出、换绑、隐藏、关闭或回池时可靠隐藏并清理状态。——Controller 在各生命周期入口统一调用隐藏和归位逻辑。
- [x] 悬浮说明窗口不阻挡卡牌射线，不破坏备战阶段拖拽、点击、悬停高亮或战斗阶段卡牌表现。——Tooltip 图形关闭 Raycast Target；战斗/备战均允许悬停，拖拽仍只在备战阶段启用。
- [x] 词条说明来自可复用的权威映射，覆盖当前全部战斗词条，文案与实际规则一致。——CSV 新增 Description，覆盖嘲讽、远射、爆裂、冲锋；规则层按配置稳定排序输出说明。
- [x] 卡牌名称和词条字号适度增大，保持现有布局与可读性。——名称最大字号 18、词条字号 15，并保留 Auto Size 下限。
- [x] 多个词条使用顿号连接，不主动插入换行；宽度不足时由 TMP 自动换行。——`FormatDisplayText` 使用 `、`，KeywordText 开启 TMP 自动换行。
- [x] 无词条卡牌不显示空悬浮窗口，单词条与多词条均正确排版。——显示入口同时检查词条集合与说明文本，测试覆盖单/多词条格式。
- [x] 资源 key 唯一且通过 ResourceApi/既有资源绑定方式使用；不引入运行时静态 UI 拼装或新的重复分配热点。——复用现有木板与号角/弓箭资源，静态 UI 仅由 Builder 写入 Prefab；撤回后的音频资源无残留。
- [x] 相关 EditMode 测试覆盖词条显示文本、词条说明与反馈配置，既有测试无回归。——目标测试先通过 13/13，最终 Builder 重建后核心结构与格式测试再通过 3/3。
- [x] BattleCardItemUiBuilder 可重复执行，并能稳定生成完整 Prefab 层级及序列化引用。——重复执行 Builder 后，最终 Prefab 结构、Tooltip 引用及 96×96 图标断言均通过。
- [x] 完成 Unity 编译、目标测试、构建/结构检查与 Console 错误检查；未进入游戏验证时明确记录风险。——Unity 编译成功，目标测试通过，最后一次 Console 错误筛选无编译/运行错误；按项目约定未进入 Play Mode。
- [x] 按当前实现同步战斗玩家设计文档、战斗卡牌美术文档、战斗 UI 与战斗系统程序文档。——四份现状文档均已同步，且已移除撤回的号角音效描述。
- [x] 结束前复核框架边界、抽象复用、任务范围和脏工作树，未覆盖的非本任务改动保持不变。——修改限定于卡牌反馈、词条配置/展示、Prefab、测试与对应文档；既有无关脏改动未覆盖。
- [x] 仅执行一次 AutoDoc/CleanupTempDocs.bat，并生成对应任务报告。——清理脚本执行 1 次并以退出码 0 完成；临时 Markdown 数量 208→208，未达到清理阈值。本报告随后创建。
