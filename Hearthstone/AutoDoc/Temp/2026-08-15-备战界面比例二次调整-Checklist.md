# 备战界面比例二次调整检查清单

- [通过] 根据用户截图重新平衡上下区域比例：卡池底框由 `1780 × 660` 收为 `1780 × 630`，仍比原版 `1780 × 600` 高。
- [通过] 将“出战 / 融合”页签继续上移到木质幕布左上角：锚点分别为 `(215, -58)`、`(525, -58)`；离线渲染确认未遮挡阶段标题。
- [通过] 将出战区卡牌整体上移：`BattleSlotList` 的 Y 从 `-170` 调整为 `-130`；离线渲染中卡牌底部与卡池框顶边约有 70 个参考像素留白。
- [通过] 保持上一轮已完成的奖励提示删除、卡池计数删除、Continue Button 单行文字和 SemiBold 中文字体不回退；Prefab 结构与相关测试均覆盖这些断言。
- [通过] 只修改一一对应的 `PreparationViewUiBuilder` 配置源，并通过 Unity Editor 调用 `Build()` 重建 Prefab；未手写 Prefab YAML。
- [通过] 更新布局结构测试断言，覆盖两个页签、出战列表、卡池面板与滚动区的新 Rect 参数。
- [通过] Unity 编译无错误；`Hearthstone.Tests.RunCardRulesTests` 通过 19/19；两份 dotnet 工程构建 0 错误；Prefab 结构查询与 1920×1080 离线渲染通过；未进入 Play Mode。
- [通过] 玩家视角设计文档：已读取基础格式，并同步木质幕布左上角页签与上下区域留白现状。
- [通过] 美术文档：已读取基础与模块格式，并同步页签位置、出战卡上提和上下比例现状；本次不新增美术资源。
- [通过] 程序文档：已读取基础与 UI 界面格式，并更新卡池面板、滚动区、页签和出战列表当前坐标/尺寸。
- [通过] 本次没有新增函数或字段，仅调整现有布局常量和对应测试断言。
- [通过] 框架边界审计：继续使用 `PreparationViewUiBuilder`、既有 View/Controller、`UiList` 与现有资源，没有引入平行布局或运行时静态拼装。
- [通过] 已完成逐项复核；下一步只运行一次 `AutoDoc/CleanupTempDocs.bat`，随后创建同名报告。
