# 备战卡池复用战斗卡片 UI 检查清单

## 需求与实现

- [ ] 备战卡池复用战斗页面的卡片视觉 UI，不再单独维护一套卡片主体表现。
- [ ] 保留备战卡池专属的未拥有状态、素材选中状态、拖拽与交互能力。
- [ ] 将备战卡池、出战槽、融合槽及其列表槽位尺寸统一到战斗卡片的宽高比例，并校正容器与间距，避免非等比拉伸。
- [ ] 备战卡池继续通过 `UiList` 和预加载映射创建条目，不在运行时拼装静态 UI。
- [ ] View 只保存序列化引用，数据绑定、交互和刷新逻辑留在 Controller。
- [ ] 调整与 Prefab 一一对应的 UiBuilder，并通过允许的 Unity Editor 通道重建/核验 Prefab；若当前环境缺少执行通道，明确记录未完成项，不手写 Prefab YAML。
- [ ] 检查相关预加载映射、页面引用和直接依赖，确保重入与对象池复用时状态正确清理。
- [ ] 检查新增函数和字段是否确有复用价值，删除不必要的一次性抽象。

## 验证

- [ ] 执行适用的编译或 Editor 测试，验证备战卡池与战斗卡片共享视觉实现且原有交互未回归。
- [ ] 检查改动范围，确保未修改 `.meta` 文件且未误改无关文件。
- [ ] 框架边界审计：未绕过 BbxCommon UI 生命周期、UiList 预加载池、Prefab/Builder 配置源或资源 API，未残留平行卡片视觉实现。

## 文档同步

- [ ] 玩家视角设计文档：读取基础格式 skill，检索现有相关文档，按实际玩家可见变化决定更新或不适用并记录证据。
- [ ] 美术文档：读取基础格式 skill，检索现有相关文档，按实际资产与视觉复用变化决定更新或不适用并记录证据。
- [ ] 程序文档：读取基础格式 skill，检索现有相关文档，按实际 UI 架构变化决定更新或不适用并记录证据。

## 收尾

- [ ] 逐项复核并补充状态与证据。
- [ ] 只运行一次 `AutoDoc/CleanupTempDocs.bat` 并记录结果。
- [ ] 清理后创建 `AutoDoc/Temp/PreparationCardUiReuse-Report.md`，记录结果、验证、偏差、风险、文档与清理情况。
