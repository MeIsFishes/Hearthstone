# 卡牌原画比例与宽度修正检查清单

## 用户要求与现状边界

- [通过] 修正当前 1～98 普通编号原画因 Unity 导入为 `1024×2048` 而产生的横向压窄视觉；实测普通卡最终 UI 网格由旧 `148.5×297` 改为 `210×297`。
- [通过] 重新绘制共享卡面的原画区域；1920×1080 离屏实卡预览显示立绘接近边框内沿，两侧无明显黑边。
- [通过] 目标绘制尺寸统一为 `210×297`，宽高比实测 `0.707071`，相对原始 `2:3` 放宽约 `6.1%`。
- [通过] 普通卡 `1024×2048` 与融合卡 `1024×1536` 分别执行 `Image.OnPopulateMesh`，最终网格均为 `210×297`。
- [通过] Unity 已对原画目录内 104 张卡牌图片执行 `ImportAssetOptions.ForceUpdate` 显式重导入；未修改任何 `.meta`，当前修正明确记录为共享 UI 的最终绘制补偿，而非 TextureImporter 修改。

## UI 与框架边界

- [通过] 仅调整共享 `BattleCardItem` 的 `ArtworkArea` 与同一 Controller 绑定表现；战斗、卡池、出战槽、融合槽仍复用同一 Prefab。
- [通过] 已同步一一对应的 `BattleCardItemUiBuilder`，并通过 Unity Editor 执行 `Hearthstone.BattleCardItemUiBuilder.Build()` 重建 Prefab。
- [通过] Builder 与 Controller 均显式设置 `preserveAspect = false`，换绑不会覆盖修正。
- [通过] 5 项定向 UI 测试覆盖卡框底边、属性徽章、底板花纹、悬停颜色、备战限定交互和共享槽位比例，全部通过。
- [通过] 框架边界审计通过：继续使用既有 View/Controller、UiBuilder、Resources、唯一预加载与对象池；未添加运行时静态层级拼装或平行绘制链路。
- [通过] 未回退并行工作区修改；`git diff --name-only` 未发现 `.meta` 变更。

## 验证与文档

- [通过] 1920×1080 参考画布下实测网格 `210×297`，普通卡与融合卡父级 `lossyScale.x/y` 均为 `1/1`，卡面左右空隙各 `20 px`。
- [通过] 普通 `1024×2048` 导入纹理和融合 `1024×1536` 纹理最终网格一致，比例均为 `0.707071`。
- [通过] 三个相关脚本校验 0 error；Unity 强制刷新与编译完成；5 项定向 UI 测试及 33 项卡牌/备战回归测试全部通过；104 张原画显式重导入后核心比例测试再次 1/1 通过；最终 Console 0 error / 0 warning；按项目默认边界未进入 Play Mode。
- [通过] 已完整读取设计文档格式及战斗系统子格式，并同步战斗、备战玩家可见构图。
- [通过] 已完整读取美术文档格式及模块文档格式，并同步战斗卡牌 `210×297` 构图规格。
- [通过] 已完整读取程序文档格式及 UI 界面格式，并同步战斗/备战共享卡面的最终绘制实现。

## 收尾

- [通过] 结束前已重新打开清单并逐项写入实现、网格测量、测试、Console 与文档证据。
- [待执行] 只运行一次 `AutoDoc/CleanupTempDocs.bat` 并记录结果。
- [待执行] 清理后创建 `AutoDoc/Temp/BattleCardArtworkAspectFix-Report.md`。
