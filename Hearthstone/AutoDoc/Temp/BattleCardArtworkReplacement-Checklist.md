# BattleCard 98 张立绘替换检查清单

## 范围与约束

- [ ] 读取并遵循主代理、`project-state-preflight`、项目修改流程与 `imagegen` 技能。
- [ ] 保留用户现有未提交和并发任务改动，后续提交对 `BattleCardCsvData.csv` 只暂存当前卡号对应行，避免吸收并发新增卡片或表头变化。
- [ ] 使用内置 `image_gen`，每张卡单独输入不同提示词；原图仅作为角色形象与轻微油画风参考。
- [ ] 每张成图保持 1024×1536 竖版、无文字、无标志、无水印，并以“种族/类型 + 卡号”（如 `Boar_001`、`GoblinBomber_002`）命名后落入 `Assets/Resources/Art/BattleCards/`。
- [ ] 不创建、编辑或删除任何 `.meta` 文件。
- [ ] 背景按 10 张循环：草地、林地、城墙、城镇、雪地、山地、草地、林地、城墙、山地；98 张合计草地20、林地20、城墙19、城镇10、雪地10、山地19，近似比例 2:2:2:1:1:2。
- [ ] 各卡形象参考对应原始立绘，姿势不完全相同，装备允许小幅变化，整体维持原有轻微油画风。
- [ ] 允许轻微配色差异：野猪可在黑、深棕、灰褐、赤棕间变化；哥布林可在草绿、黄绿、橄榄绿间变化；食人魔可在黄绿、土黄、灰黄、深灰近黑间变化，同时保持类型可识别性。
- [ ] 每完成一张：目视检查成图、写入卡表对应 `ArtworkKey`、仅提交本张图片与卡表、立即推送 `origin/main`。
- [ ] 框架边界审计：沿用现有 `Resources/Art/BattleCards` 与 `ArtworkKey` 资源读取流程，不手写 Unity 资产 YAML、不绕过资源框架。

## 逐卡替换与推送

<!-- 背景按上述 10 张循环，CardTypeId 与角色类型以 CSV 为准。 -->

- [ ] 卡 01：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 02：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 03：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 04：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 05：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 06：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 07：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 08：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 09：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 10：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 11：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 12：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 13：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 14：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 15：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 16：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 17：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 18：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 19：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 20：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 21：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 22：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 23：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 24：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 25：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 26：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 27：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 28：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 29：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 30：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 31：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 32：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 33：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 34：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 35：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 36：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 37：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 38：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 39：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 40：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 41：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 42：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 43：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 44：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 45：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 46：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 47：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 48：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 49：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 50：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 51：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 52：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 53：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 54：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 55：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 56：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 57：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 58：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 59：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 60：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 61：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 62：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 63：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 64：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 65：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 66：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 67：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 68：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 69：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 70：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 71：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 72：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 73：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 74：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 75：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 76：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 77：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 78：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 79：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 80：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 81：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 82：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 83：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 84：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 85：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 86：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 87：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 88：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 89：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 90：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 91：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 92：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 93：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 94：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 95：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 96：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 97：生成独立立绘、检查、更新 CSV、提交并推送。
- [ ] 卡 98：生成独立立绘、检查、更新 CSV、提交并推送。

## 结束审计

- [ ] CSV 的前 98 张卡仍唯一连续，1–98 的 `ArtworkKey` 均唯一且对应文件存在；不改动并发任务新增的第99张及以后卡片。
- [ ] 统计背景数量和五类角色数量，并核对全部图片尺寸、格式与可读取性。
- [ ] 核查 `git status`、98 个任务提交与远端同步状态，无无关文件混入。
- [ ] 玩家视角设计文档：完整读取基础格式 skill，核验是否受实际资源替换影响并处理。
- [ ] 美术文档：完整读取基础格式 skill，核验是否受实际资源替换影响并处理。
- [ ] 程序文档：完整读取基础格式 skill，核验是否受实际资源替换影响并处理。
- [ ] 结束前重新打开本清单，逐项填写通过/未通过/不适用及证据。
- [ ] 只运行一次 `AutoDoc/CleanupTempDocs.bat`，随后创建对应 `BattleCardArtworkReplacement-Report.md`。
