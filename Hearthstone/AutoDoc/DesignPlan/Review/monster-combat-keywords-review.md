## 1. 实现方式

以配置驱动词条集合，统一战斗结算、融合继承与四类卡牌条目的显示数据。

## 2. 验收

### 2.1 验收方式

用户在 2026-08-15 明确免除进游戏验收，本篇改用代码逻辑、配置接入、Unity 编译、EditMode 测试和资产编排检查收口。主代理复核 `BattleKeywordRulesTests` 作业 `6a6289081c4d41cfaeffe1eee0427d59` 为 9/9 通过；最终全 EditMode 作业 `32dd621e7dfa4ba78a9bb9f546d52c37` 为 55/56，唯一失败是任务前卡 1 原画键 `Boar_001` 与旧断言 `Boar` 不一致，与本篇词条逻辑无关。Unity 刷新后无编译错误，Console 无 Error。

### 2.2 美术资产验收

- `ART-01`：通过。`BattleCardItem`、`PreparationCardItem`、`PreparationSlotItem`、`PreparationFusionSlotItem` 四个正式 Prefab 均引用同一动态 NotoSansSC 字体和独立 `KeywordText`；统一格式器对 0 词条输出空白、1 词条输出单行、4 词条固定输出“嘲讽 · 远射\n爆裂 · 冲锋”。四个 Builder 已经公开入口重建，主代理重跑 Builder 后确认字体包含“嘲讽远射爆裂冲锋”全部字形，未手写 Prefab、Scene、FontAsset 或资源索引。

### 2.3 程序功能验收

- `FUNC-01`：通过。类型 CSV 映射战士/弓箭手/投弹手/野猪/食人魔至嘲讽/远射/爆裂/冲锋/None，显示和战斗均消费同一实例集合；配置解析测试通过。
- `FUNC-02`、`FUNC-03`：通过。候选掩码只保留存活嘲讽单位；无嘲讽时恢复全部有效目标，多嘲讽仍交由既有随机目标源选择；目标规则测试通过。
- `FUNC-04`、`FUNC-05`：通过。远射比例、向下取整和反伤门控来自 `BattleKeywordCsvData`；定向测试覆盖奇数、零伤害及自定义比例/门控。
- `FUNC-06`：通过。爆裂距离和比例配置驱动，只处理物理相邻的存活槽，主目标不重复受伤，死亡统一延迟提交；邻接与空位测试通过。
- `FUNC-07`：通过。冲锋在伤害前按配置增加存活友军攻击、最大生命和当前生命，死亡单位不参与；Component 镜像及自定义增益测试通过。
- `FUNC-08`：通过。融合对素材当前词条取规范化去重并集，None 不贡献；首次融合生产链与幂等并集测试通过。既有规则禁止 99 再作为素材，因此策划中“99 后续继续融合”的条件分支不可达，Plan 已明确保留该生产禁令。
- `FUNC-09`：通过。多词条伤害按远射主伤害后再计算爆裂，比例逐步向下取整，主目标和相邻目标均按同一配置快照结算。
- `FUNC-10`：通过。生产顺序固定为冲锋增益、嘲讽选目标、远射/爆裂伤害、反伤门控、统一死亡提交与胜负判断；四词条集合去重且每项只执行一次。

## 4. 详细审查意见

本篇唯一代码审查 `AutoDoc/Temp/monster-combat-keywords-code-review-round-1.md` 初始结论为不通过，指出战斗数值硬编码与 CSV 双轨、正常 Continue 错用显式 Scenario、类型词条校验不足、共享字体缺字及四词条单行布局。成立项均已修正：词条比例、距离、增益和反伤门控统一迁入 `BattleKeywordCsvData`；正常 Continue 恢复 `Scenario=null`；非法/组合初始词条被拒绝；四词条固定两行；共享字体通过既有 TMP/Builder 流程补字。用户要求每类审查最多一次，因此未追加同类复审，主代理以最终差异、9/9 定向测试、55/56 全量回归、公开 Builder 重建和只读资产检查完成核对。

实现保留公开 `int Attack` 并同步 `AttackValue`，显式场景 DTO 采用防御性复制，默认战斗保持原随机初始化；没有新增平行 Battle System、Stage、Task 图、运行时 UI 拼装或手写 Unity YAML。已知全量测试唯一失败属于任务前原画键改名。按用户最终限定的代码逻辑验收口径，本篇结论为通过。
